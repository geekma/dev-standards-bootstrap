# 标准升级日志（STANDARDS CHANGELOG）

> 本文件是 `DEVELOPMENT_STANDARDS.md` 的**版本升级历史唯一落点**（规则权威见规范正文 §2.14，v3.8.0 起自规范正文外迁）。
> 更新语义：追加式——**新条目置顶**，历史行不改写（§2.15）；存量回填义务与影响清单见各行末列。
>
> **归属（v3.16.0）**：本文件**归 Skill 所有**——由 `bootstrap --core` 随规范下发、由 `--upgrade` 整体更新。**目标仓库请勿在此追加本地条目**：你的变更历史写 `docs/changes/<变更号>/09-changelog.md`（§4 格式），缺陷索引写 `docs/bugfix-log.md`。若已追加，`--upgrade` **检测到即拒绝**（fail-closed，`--force` 可越过）——既不静默覆盖，也不做追加式合并。

## 标准升级日志

| 版本 | 日期 | 变更条款 | 影响既有文档（须回填） |
|---|---|---|---|
| v3.56.1 | 2026-09-25 | **A6 族 CI soft 化（CHG-070，REQ-1006，用户裁定方案 b）**：CI source-audit 自 2026-09-22 恒红根因=.gitignore `docs/*`（CHG-017/FU-032 裁定）与 A6a/b/c（依赖 docs/ 磁盘态）结构性冲突——A6 三断言并入 REQ-965 soft 桶（AGENT_GUARD_AUDIT_SOFT=1 降警，本地恒硬，A3/A3c/A10 同款）；A6d/e 无需包（CI 空 docs 天然过）；A10 分母/基线不变。**方案 a（恢复审计链 git add docs/）被用户否决**——docs/ 忽略裁定保持。**边界声明+铁律入册（同日重评估，用户维持原裁定）**：本 soft 化仅作用源仓 audit（源层件不下发）——目标仓 docs **必须入库**且由 shipped 执法面保证（与本裁定刻意区分）；源仓 docs/ 为本地存在不入库（CHG-017 维持）；**源仓自身变更不得影响通用能力语义——MAINTAINER §9 立规，foundation L1 已记账**。**`--upgrade` 零分发变化**（audit 为源层件）。**回填义务：无** | **已接入项目**：无（audit 不随 --upgrade 下发）。**回填义务：无** |
| v3.56.0 | 2026-09-25 | **07 评审署名/溯源机校（CHG-069，REQ-1005，用户批准收口建议 1）**：执行违规（CHG-065~068 自评冒充评审，BATCH-20260925 披露节）机制化闭环——**GATE-E85**：validate_delivery 内 07 存在时必含 §2.1.7 签名行且第三段为可追溯子任务 id（`task-…`，形态对齐 E82）；无独立子任务 id 的自评=门禁 5 违规拒交付；07 缺席不外溢（L0 最小集路径不变）；E85 单 die 点（T41 不受扰）；DS 阶段 8 验收行同步；audit +A38×1；golden T46 三断言（534→537）+T44 三态（check-confirm 协议随 CHG-068 收口迁入）。**`--upgrade` 带走 agent-gate**。**回填义务（一次性）**：升级后首个交付变更的 07 须含签名+task id（无子任务能力的环境按 §2.1.7 登记可追溯执行主体）；历史变更不回填 | **已接入项目**：`--upgrade` 带走 agent-gate。**回填义务（一次性）**：见左 |
| v3.55.0 | 2026-09-25 | **治理脚本单源化批（CHG-068，FU-904 四候选全做，用户裁定"全做"）**：①**require_section_or_decl helper（REQ-1001）**——E80/E81/E84 三兄弟检查形状单源（延伸发现/历史相似检索 未命中正则的 E81 内联拷贝退役），die 消息与码逐字节不变（T37/T44 零适配）；②**check-confirm 子命令（REQ-1002）**——confirmation_gate_reason 函数抽取（E83 语义单源：独立/批次锚点 scope+机校标记+L0-exclusive+env 豁免），`agent-gate check-confirm <id>` 只读 exit 0/1/2（缺参 exit 2 非 die 形态，T41 域不受扰）；new-change 删 confirm_ok（−2.2KB 双实现）改调子命令，NC-E10（gate 缺失/执行错误）新增；③**append_diag_lines fixture helper（REQ-1003）**——run-tests 散点诊断夹具收敛（8 处独立语句），heredoc 单源（seed_artifacts/seed_batch）保持字面；④**install-hook-adapter 尾行（REQ-1004）**——生成成功后提示 .agent-state/ 属运行态建议 gitignore；⑤golden 530→**534**（TC-1056/1057 exit 协议两态+TC-1060 提示行断言+外溢/自锚点负例随迁）；T25 补拷 gate（wave-2 硬依赖）。**`--upgrade` 带走 agent-gate/new-change/install-hook-adapter**。**回填义务：无**（行为等价重构；check-confirm 为新增可选探针） | **已接入项目**：`--upgrade` 带走三脚本。**回填义务：无** |
| v3.54.0 | 2026-09-25 | **历史相似检索机校 + 防回归补测 + 减法收尾 + FU-903 关闭（CHG-067，用户裁定"全部执行（推荐）"）**：①**GATE-E84（REQ-998）**——阶段 2「历史相似检索」升 A 层机校（02 必含节或「未命中（检索词）」声明行；BUG 轨并入 E81 组合检查），对齐 E80/E81 同形先例；audit +A37×1；②**防回归补测（REQ-999）**——TC-1050（staged code-only+工作树缺 00.5：无豁免拒/ALLOW_UNCONFIRMED 过——F5 三处豁免的执法面闭环）+TC-1051（批次自锚点空记录→E83）；golden 521→**528**（T44 八断言）；③**DS 减法收尾（REQ-1000）**——§2.5 阶段 6 批次句/§2.2 能力卡尾指针化 −~900B；A11 166,099→**165,727B（余量 2,209B）**；④**FU-903 关闭（政策裁定）**——SLOG 保持追加式冻结（无上界压力+A25/T40 pin 面+跨仓 diff 可读性；~30-45KB 潜在减法收益不值 §2.15 政策冲突风险），**⑤收口 review 再审计（同日，三只读子代理）**——修复 P2×2：new-change `confirm_ok` 兄弟名单管道优先级 bug（`||` 低于管道致 others 残换行 → awk stop 失效 → 兄弟确认外溢，与 fail-closed 承诺相反——子壳内 `|| true` 形态修复+TC-1055 负例）；DS §2.17 双轨段补「变更目录解析顺序」（压缩指针曾落空）；对齐 P3：§2.17.2d 补机校标记口径/§2.5 阶段 8 必含清单补「分阶段评审留痕」（SLOG v3.53 声明落点对齐）/§2.2 缺陷轨句降指针/C4 速查表注/P01·P02「7 件」活残留→8 件+注记；**FU-904 登记**：E80/E81/E84 helper 合并、confirm_ok 下沉 gate check-only 子命令、run-tests fixture 函数化（23 处）、.agent-state .gitignore 提示落点——均行为面/结构性改动，待用户裁定。**`--upgrade` 带走 agent-gate**。**回填义务（一次性）**：升级后首个新变更 02 须含「历史相似检索」节或声明行（bug 轨 01-diagnosis 同）；历史变更不回填 | **已接入项目**：`--upgrade` 带走 agent-gate（规范零改动=仅页脚）。**回填义务（一次性）**：见左 |
| v3.53.0 | 2026-09-25 | **分阶段评审留痕 + 减法批（CHG-065/066 同批，用户 2026-09-25「全部执行」三轮沟通确认）**：①**分阶段评审留痕（REQ-993，§2.2 新段）**——阶段 1-5 各阶段评审批结论**必须派发独立子代理**（输入契约同段）并在 07「分阶段评审」节逐阶段留痕（阶段/独立主体/结论三列）；L0/L1 单行声明 `- **分阶段评审**：未命中，不适用（<理由>，合并至阶段 8 终审）`（对齐 §1.1 形态）；未留痕未声明=阶段 8 退回项；§2.5 阶段 8 验收行同步；AGENTS.md 硬指令「禁止只做阶段 8 终审的收敛执行」；②**FU-902 处置（方案 b，REQ-993 内嵌）**——§2.17.2d/§2.2 明文：缺陷轨机校点=绑定 CHG 的 begin（GATE-E83）；独立缺陷直修必须先建 CHG 并 `bug_ref` 绑定（六件套不再扩确认契约）；③**减法批（REQ-995/996/997，零行为变化）**——SKILL 五节压缩 −5.6KB（20,427→14,807）、gate 叙事注释 −1.9KB（79,992→78,146，A21 pin 字面量 `no longer a PARSER`/`style rule, not a load-bearing one` 原样保留）、DS §1.1 批次行压缩 −2.3KB、AGENTS 门禁节 −1.3KB；A11 167,089→**166,099B（余量 1,837B）**；④**执法面**——audit +A36×4（§2.2 锚点/阶段 8 行/缺陷轨句/AGENTS 指令）+golden T43 五断言（516→521）+`.audit-baseline` 394→398；⑤**FU-903 登记**：SLOG 政策门（62 行超纲叙述 ~30-45KB 减法，需 §2.14 追加式政策变更=独立用户决策）。**§5.2 加一删一**：+2 REQ（993/994）对等减法=SKILL/gate/DS/AGENTS −10.9KB（减法批即对冲）；破坏面=阶段 1-5 评审留痕成为交付义务（新变更起），存量不回填 | **已接入项目**：`--upgrade` 带走规范/AGENTS/SKILL/agent-gate。**回填义务（一次性）**：升级后首个新变更的 07 须含「分阶段评审」节或 L0/L1 单行声明；历史变更不回填 |
| v3.52.0 | 2026-09-25 | **沟通先行门（CHG-064，用户指令 2026-09-25「先沟通、文档先行、再执行，顺序不要颠倒」，三轮沟通后用户裁定方案 B 独立产物）**：①**顺序铁律（REQ-988，§2.17.2d 新条款）**——变更与缺陷修复一律 **深挖分析 → Round-1 沟通稿（00.5）→ 用户整体确认 → 批量文档（01-spec 起正文）→ 编码 → 测试**；确认记录落盘前禁止填写后续产物正文与源码编辑；②**第 15 件契约产物（REQ-989）**——`00.5-communication.md`（模板 `resources/templates/communication.md` + entry 骨架 `templates/entry/00.5-communication.md`；批次 `## <变更号>` 锚点同构；承载深挖结论/影响/风险/≥2 候选方案取舍/测试思路/**用户整体确认记录**（追加式）），契约枚举面 14→15 全链同步（§1.1 批次/不可配置项/落点表/§2.15 语义表/SKILL/stamp 注释/双 README 树/agent-governance.yml/artifact-pipeline/audit 注释）；③**gate 三层执法（REQ-990）**——begin `required_docs` 7→8 + **GATE-E83**（00.5 缺失→E03 点名；无「用户整体确认记录」节/无确认标记→E83；**批次成员按本变更锚点小节校验，小节跑到下一兄弟锚点（权威名单取 ids），兄弟确认不外溢；L0 单行声明 `- **沟通确认**：未命中，不适用（理由）` 等价放行；豁免 `AGENT_GUARD_ALLOW_UNCONFIRMED=1`（required_docs 同键跳过）+09 登记）**；validate_delivery 15 件（stamp `--all` 天然覆盖）；④**new-change 两段式（REQ-991）**——wave1 只生成 intent/governance/00.5（首跑 notice 不阻断），`confirm_ok`（与 E83 同锚点串）在位才补齐 01-spec 起五件；重跑未确认 die `NC-E09`；`--allow-unconfirmed` 显式越过（自动化入口）；⑤**索引与载体（REQ-992）**——CLAUSE_REGISTRY +R43（universal，行数 42→43）；v3.52.0 载体全链（SKILL ×4 含 frontmatter/DS 页脚/CHANGELOG 本行/README ×3+树/MAINTAINER/METHODOLOGY×1+methodologies×5/plugin+marketplace）；⑥**执法面**——golden 492→514（T42：E83 独立/批次锚点两态/豁免/L0 声明/new-change 两段式三态/模板装载 pin）+T25 两段式适配（七件断言改 3→8+NC-E09/NC-E07）+T39 注册表计数 42→43+T14/T18/T18b/T19/审计夹具补 00.5+seed_artifacts/seed_batch_artifacts 内置确认标记；源层 pin：A25 七件→八件、A26 '14 件'→'15 件'、主线 pin 串更新。**§5.2 加一删一豁免登记**：+5 REQ 对等减法=新执法能力（沟通确认为管线最高价值人工授权证据，独立成件无既有条款可裁；永久文档税由 L0 单行豁免通道对冲）。**规范条款级破坏面**：begin 对非豁免变更新增 00.5 存在性与确认记录校验——存量仓升级后首个新变更须补（一次性，骨架由 new-change 生成；同 v3.39 评审输入先例） | **已接入项目**：`--upgrade` 带走规范/AGENTS/communication.md 模板+entry 骨架 8 件/agent-gate/new-change/bootstrap/CLAUSE_REGISTRY。**回填义务（一次性）**：升级后首个新变更 begin 前按新模板补 `00.5-communication.md`（L0 可单行声明）；历史变更/存量批次不回填。 |
| v3.51.0 | 2026-09-24 | **安装清单：全文件用户改动检测与保留（CHG-063，用户指令 2026-09-24 追问「其他有用户改动的文件都要处理」=CHG-062 可否决点①否决→通用化）**：①`.dev-standards-manifest`（target 根，cksum/路径行式，POSIX 零依赖）记录每载荷文件自安装基线；②--upgrade 差异文件逐个核对：hash 匹配=纯版本漂移→updated+刷新；不匹配=用户改过→**kept 保留+显式报告**（--force 显式越+警告+刷新）；无条目（legacy 仓）→保守 kept+提示 --force 一次同步采纳清单；③四层叠加不替代：LIVE_SKIP（数据件）/AGENTS 托管块合并/安装清单/CONFLICT 各司其职；④执法面：golden 485→**492**（清单在位/用户改 kept+逐字节保/force 越+刷新/版本漂移 updated/legacy 两态）、源层 +A34×2（393→395）。**回填义务：无** | **已接入项目**：`--upgrade` 带走 bootstrap+规范；**首次 v3.51.0 --upgrade 无清单=差异文件全 kept（保守），`--force` 一次同步并采纳清单，此后升级智能归因**。**回填义务：无**。 |
| v3.50.0 | 2026-09-24 | **安装/升级用户内容保留（CHG-062，用户指令 2026-09-24「含用户信息不要替换，比如 Agent 文件」）**：①resources/AGENTS.md 托管标记块（begin/end 注释行）+bootstrap.sh `merge_managed`（awk 三段拼接：dest 头区+src 托管区+dest 用户尾区）；②`agents-md` 键=merge-or-keep——有标记只更新标记区（用户区块原样保留，报告 merged）；无标记差异文件=保留原文（kept，**--force 不越**，删除文件重装=唯一强制刷新通道）；③通用文件 CONFLICT/--force 语义零变化（T11 用例移 METHODOLOGY 承载）+T21 TC-144 语义升级（用户 AGENTS.md=kept+安装完成，不再 CONFLICT 拒绝）；④执法面：golden 480→485（fresh 标记/merged 保用户区/legacy kept 含 --force 两态+METHODOLOGY 回退面）、源层 +A33×2（391→393）。uninstall 无需动（AGENTS=Tier3 已保留）。**回填义务：无** | **已接入项目**：`--upgrade` 带走 bootstrap/install 源层件+规范；既有目标仓 AGENTS.md 无标记者升级时 kept（可见提示），采纳标记=删除该文件重装一次。**回填义务：无**。 |
| v3.49.0 | 2026-09-24 | **版本载体全链清单条款（CHG-061，CHG-059 可否决点③用户批准+A/B 数据支持）**：①DS §2.14 新增「版本载体全链清单（v3.49.0）」——版本 bump 载体全链唯一权威枚举（页脚×9+分发元数据×2+README×2 各 3 处+升级记录×2；新载体入链/出链同版本修订本条；历史行与条款溯源标记不在 bump 面），fresh agent 载体 bump 从全仓侦探降为读一条款（CHG-060 A/B 覆盖外遗漏①收口）；②注册表 +R42 universal 行（41→42，universal 11→12）；③**§5.2 对等减法**=DS §2.16.5「标准升级」落点行退役（信息被新条款+阶段10 行交叉引用覆盖，合并减法序 #1，A32 反向断言 pin 缺席态防复活）；④gate die 码提取域统一 `[0-9]+`（T30×3+T41，CHG-060 P3-1 收口，现行 2 位码行为逐字节等价）；⑤执法面：golden T39 计数 42、源层 +A32×2（389→391）。**回填义务：无**；A11 实测 165,247B（余量 2,689B） | **已接入项目**：`--upgrade` 带走规范/注册表即可（run-tests 为源仓件）。**回填义务：无**。 |
| v3.48.1 | 2026-09-24 | **gate 检查码重编号（CHG-060，CHG-058 P3-6 遗留收口+fresh A/B 实测载体）**：①CHG-058 新增两检查码与既有 stage-3 依赖/里程碑码重号修复——stop 轨 02 延伸发现检查 E15→**GATE-E80**、begin bug_ref 轨 01-diagnosis 延伸发现检查 E16→**GATE-E81**（检查逻辑/正则/执法语义零变化）；执行期延伸发现并入同族预存重号——签名检查 E52→**GATE-E82**（stub 检查 E52 保留，T30/T37 码级 pin）；旧 stage-3 E15/E16 保留。②gate die 码唯一性 golden（**T41**：die 消息码域 `uniq -d`=0，注释字面量不计；479→480）。③fresh-session 纯包 A/B 实测数据落账（FU-106⑦ 全闭合，登记于 CHG-060 09 节并显式引用 CHG-059——数据供可否决点③「载体清单入 universal」裁决）。**规范条款级破坏面：无**（纯执法工具标识重编号）；**回填义务：无**；golden 479→480，源层断言 389 不变（基线 388） | **已接入项目**：`--upgrade` 带走 agent-gate/run-tests 即可（die 码变更对存量变更无感——仅排障消息码面）。**回填义务：无**。 |
| v3.48.0 | 2026-09-24 | **缺陷/变更闭环三落点（CHG-058，CHG-057 裁定方案 A）**：①**延伸发现即时落盘（REQ-975）**——DS §2.5 阶段 2/6 各加「延伸发现」节（三行处置：并入本变更 \| FU \| 忽略须 09 理由；无发现写"未发现"）+ §2.12「会话内发现即登记，禁止攒到收尾」+ gate **GATE-E15/E16**（begin/stop 双段执法：02 存在时/bug_ref 轨 01-diagnosis 必含，文件级机校；E15/E16 与既有 stage-3 依赖码重号——下版本重编号，登记可否决点）。②**历史相似检索步（REQ-976）**——阶段 2/6 必含「历史相似检索」节（检索 bugfix-log+docs/bugs+docs/changes，检索词/命中号/裁决三要素；无命中写"未命中（检索词）"防假检索），与同族推演互补（推演=空间旁路、检索=历史台账）；评审 B 层核对检索质量；entry 骨架模板同步两节。③**历史关联必填（REQ-977）**——七要素⑦扩「历史相似」+bugfix-log 模板字段行+§2.16.5 勘误新开组显式引用原 CHG 号+**G4 增量执法**（最新 BUG 条目须含历史相似，存量不追=升级零回填义务）。④**执法面（REQ-978）**——golden +5（E15/E16 两态+G4 负态，fixture 全量补节防连坐）；源层 +A30×6（DS 节）+A31×4（G10 枚举面 pin——README/SKILL G10 同步漏更复发防线，A29 同族+模板 pin）；entry/02 模板补两节。**规范条款级破坏面：无**（新增节/列为声明式最小形态，存量产物不含该节属合法空转——gate E15/E16 仅对本次 begin 后的活跃变更执法）；**回填义务：无**（G4 增量执法设计）；A11 实测 164,797B/上界 167,936B 余量 3,139B；§5.2 预算豁免：+4 REQ 对等减法=三落点均为增量执法最小形态+历史台账闭环补齐同族推演盲区+A31 属 A29 同族枚举面防复发，无既有条款可裁。golden 474→479（E15/E16/G4 态），源层断言 379→389（A30×6+A31×4，基线 388） | **已接入项目**：`--upgrade` 带走规范/agent-gate/audit-docs-consistency/bugfix-log 模板/entry 骨架/AGENTS。**回填义务：无**（存量 bugfix-log 不校历史相似列；新条目起按模板）。 |
| v3.47.0 | 2026-09-24 | **注册表类型扩面（CHG-057 批 2，Top1 规范倒置第二步）**：①**注册表（REQ-972）**——CLAUSE_REGISTRY 28→41 行：L1-standards +6（§1.1 统一编号/§1.2 RTVM 矩阵文件/§2.5 分阶段产物规格/§2.14 存量回填/§2.16.5 落点总表/§4 Changelog 格式）、L2-architecture +5（§2.7 发布上线/§2.8 监控告警/§2.9 AI/LLM 专项/§2.10 测试数据隔离/§2.13 供应链与接口）、L3-critical +2（§2.7.3 回滚决策/§0.5.1 风险分级表）；锚点写行前 grep 验真=1，骨架行 R21-R25 保留；L1/L2/L3 取包从 1-3 条款骨架变为全集。②**golden（REQ-973）**——T39 行计数 28→41+五类型最小集下限（L0≥12/L1≥9/L2≥6/L3≥3/universal≥11，最小集形态防"全集"锁死）；T40 fixture 扩 L1/L2/L3 正常生成态。③**A11 口径修正（REQ-974）**——MAINTAINER §5.1「当前」句 144KB→164KB/167,936B（v3.43.0 CHG-053 重校漏同步）+ audit 注释同步 + A28 断言防复发；余量实测 4,718B。④**A11 体量**——本版 DS 正文零改动（仅页脚版本号），DS 减法留批 3+（A/B 实测数据裁决）；§5.2 预算：+3 REQ 对等减法=L1/L2/L3 手工锚点对照阅读路径被注册表包替代延续。**规范条款级破坏面：无**（注册表数据行增量；不取包=零行为变化）；golden 463→474（T39+5/T40+6），源层断言 376→379（A28+A29×2 SKILL 清单枚举面——评审后 sweep 补 SKILL 核心/强制层清单两行并装机校防复发，基线 378） | **已接入项目**：`--upgrade` 带走注册表数据行+MAINTAINER（无新分发件）。**回填义务：无**。 |
| v3.46.0 | 2026-09-24 | **条款注册表与阅读包（CHG-056 批 1，Top1 规范倒置第一步）**：①**注册表（REQ-968）**——新增 `resources/CLAUSE_REGISTRY.md` 条款机器索引层：行 ID `Rxx`（无连字符形态，不进 REQ/DES/TC/SC/BUG/CHG/FU 编号面，A6c/G2 不误扫）、`文件名::唯一锚点串` 权威落点（写行前 grep 验真=1）、类型标签封闭词表（L0-bugfix/L1-standards/L2-architecture/L3-critical/universal）、强制级别、MAINTAINER §5.3 生命周期四类；首批 28 行（L0-bugfix 全量 + universal/L1 骨架）；DS 仍是条款正文唯一叙事权威，注册表禁止复述正文。②**生成器（REQ-969）**——`generate-reading-pack.sh <type> [--verify]`：类型过滤（含 universal）→ 逐条锚点存在性+唯一性解析 → **fail-closed**（文件缺失/失配/多命中退出 3 指名 Rxx；未知类型 2；零条款 4）；落点多候选解析（源仓 resources/、安装仓 docs/、仓根）；`--verify` 零输出可入 CI。③**L0 bugfix 阅读包（REQ-970）**——按需生成 ~23 条款路由切片，**禁止预生成入库、禁止手编**（预生成=双权威源）；golden T39（注册表形态五断言）/T40（生成器九态 + 接线静态 pin）。④**接线（REQ-971）**——DS §2.17.2c 新条款；AGENTS 锚点表路由行（锚点表=路由入口、阅读包=内容切片）；bootstrap --core 装注册表 → `<docs>/CLAUSE_REGISTRY.md`、--guard 装生成器 → `<scripts>/`；uninstall Tier1/Tier3 分层；agent-gate `is_code_path` 豁免生成器。⑤**A11 体量关系**——本版 DS 仅增 §2.17.2c，减重本体留批 2+；§5.2 预算豁免：+4 REQ 对等减法=注册表+生成器替代手工锚点维护的阅读税路径（批 2 起 DS 减重为 Top1 主收益）。**规范条款级破坏面：无**（新增可选机制；不跑生成命令=零行为变化）；golden 441→463（T39×6+T40×16），源层断言 366→376（A27×10，基线 375） | **已接入项目**：`--upgrade` 带走 CLAUSE_REGISTRY.md/generate-reading-pack.sh/规范/AGENTS/agent-gate + bootstrap。**回填义务：无**。 |
| v3.45.0 | 2026-09-24 | **断言预算制（CHG-055，Top3 + PM 配套裁定）**：①**audit soft 桶（REQ-965）**——新增 `report_soft`（warn 不计 fail、不入 A10 分母）+ `AGENT_GUARD_AUDIT_SOFT=1` CI 开关：A3 数字闭环/A3c 同步/A10 基线三断言 CI 降 warn 不阻断，本地默认恒硬；ci.yml source-audit 接线；②**断言退役净减 3（REQ-966）**——「section5 same-family interceptor」（与关键词矩阵同族推演循环双形状）、「methodology 3.2 implicit-link row」（矩阵循环 METH 落点重叠）、A6c 自指 pin（grep 本文件自身字符串恒真）——不变量均由矩阵循环/目录扫描承载，退役有痕；③**断言生命周期（REQ-967）**——MAINTAINER §5.3：四类标注 + 近两版无捕获→降 soft 观察→退役阶梯（结构/行为类不适用）+ soft 桶滥用防线（每次调用 09 登记）；**体系内歧义分级裁定（REQ-967）**——DS §2.1 规则 8 补：门禁语义/证据边界歧义必用户裁定，表达/落点/细节歧义执行方按「最保守可逆」自裁 + 09 登记可否决点（bus factor 1 解法，AGENTS 锚点行同步）；④**加一删一（§5.2）**：+3 REQ 对等减法=断言净 −3 + §5.3 阶梯为未来断言装退役通道；豁免理由=治理元条款（分级裁定规则）降低后续所有变更裁定成本；⑤golden 436→441（T38 五断言），源层 369→366（退役 3），基线 365 | **已接入项目**：`--upgrade` 带走规范/AGENTS/MAINTAINER（audit/ci.yml/run-tests 为源层件不下发）。**回填义务：无**——soft 模式仅本仓 CI 接线；目标仓模板 workflow 不引用 audit-standards-src。**规范条款级破坏面：无**（分级裁定为放宽方向；退役断言均有同不变量承载） |
| v3.44.0 | 2026-09-23 | **维护税装牙批（CHG-054，用户 2026-09-23 优化 roadmap Top2）**：①**L0 最小不可占位集 5→3（REQ-962）**——`04.5`/`05` 对 L0 并入轻量通道单行声明（形状同 §1.1：`- **<标记>**：未命中，不适用（<理由>）`），gate validate_delivery 按 risk_level 分支（缺失/非 L0 走原硬校验，fail-closed；非 L0 仅声明行空壳 GATE-E52 拒绝），L1 五件不变；②**08-supplement 并入 09（REQ-963）**——阶段 9.6 改「补充说明」节落 09 CHG 条目内，产物集 15 件→14 件（契约名句/批次条款/编号清单/落点表/双 README 图注/SKILL/stamp 注释同步；存量 08 文件与 SLOG 历史行不回改）；③**PART B 版本快照改 CHANGELOG 派生（REQ-964）**——A25 六条手写版本 pin 删除改 SLOG 派生组（行数下限 ≥8 + 严格降序 + 首版行==页脚版本），PART B 四关键词快照块转正永久矩阵脱版本化，升级 v3.45.0+ 审计脚本零手改；④**加一删一（§5.2）**：+3 REQ 对等减法=阶段 9.6 独立产物废止（15→14 件）+ 4 手写快照块退役 + 6 手写版本 pin 删除，净减法；⑤golden 431→436（T37 三态+文案+评审 P1 变体负例），源层断言 366→369（A26 +7、A25 6→2），基线 368 | **已接入项目**：`--upgrade` 带走 agent-gate + 规范/AGENTS（audit-standards-src 为源层件不下发）。**回填义务：无**——L0 声明路径只影响新变更；存量 08 文件=历史合法形态。**规范条款级破坏面：08 独立产物废止（存量合法）；L0 交付校验放宽（显式声明路径，非静默缺省）** |
| v3.43.0 | 2026-09-23 | **缺陷发现入口扩展 + A6c 放宽（CHG-053，FU-106④ + FU-111 闭合，用户裁定"全建 CI/会话交互/完整六件/频控要"）**：①**回归红自动六件套（REQ-958）**——新脚本 `bug-autointent`：失败指纹（规范化失败项集合 cksum）+ 频控闸门（`AGENT_GUARD_AUTOBUG_WINDOW` 默认 86400s，窗口内同指纹不新建、仅向原 01-diagnosis「复现记录（自动）」追加复现行；台账 `docs/bugs/.autofingerprint.tsv` 隐藏非 md 不入 G8）+ 完整六件套骨架（docs/bugs/BATCH-<UTCday>/ 扁平锚点，六模板锚点化+元数据预填，severity 不声明=六件全查）+ bugfix-log 置顶双登记 + 无 git 显式降级 exit 2；workflow 模板 `github-regression-to-bug.yml`（workflow_run 监听测试 workflow、failure 且默认分支，薄调用）；bootstrap --guard/uninstall Tier1 清单同步；②**会话内交互三选项（REQ-959）**——session-gate start 审计红灯时输出交互提示（建骨架/登记 FU/忽略须 09 理由，软执法 fail-open，机制下沉零新文件）；§2.17.2b 新条款 + AGENTS 门禁 4 句；③**gate 摩擦进 metrics（REQ-960）**——die 事件账 `.agent-state/gate-friction.tsv`（append-only TSV，fail-open）+ metrics 子命令按错误码聚合并入；④**A6c 编号集放宽（REQ-961，FU-111 闭合）**——独立 CHG-* 目录 ∪ BATCH-*/ 锚点成员（去重十进制），批后独立目录不再误报跳号；⑤§5.2 预算豁免登记（4 REQ + 2 模板件=新能力面无可对等裁剪；部分减法=REQ-959 机制下沉）⑥**A11 体量上界重校准 156KB→164KB**（§2.17.2b 增文 160,160B 推破 159,744B；KiB 步进 +8KB，MAINTAINER §5.1 记录）⑦golden 419→431（T34 九态/T36 三态），源层断言 359→366（A26 +7 pin），基线 365 | **已接入项目**：`--upgrade` 带走 bug-autointent/regression-to-bug.yml/session-gate/agent-gate + 规范/AGENTS；workflow 的 `workflows:` 列表按仓测试名修改后启用，不启用=零影响。**回填义务：无**。**规范条款级破坏面：无**（新增信号通道；A6c 为放宽方向） |
| v3.42.0 | 2026-09-23 | **追踪矩阵单源派发切片（CHG-051，FU-106③）**：①**§4 追踪矩阵映射自动派生（REQ-955）**——`stamp-provenance.sh` 新增 `--trace <变更号>` 单跑并入 `--all`：从变更 09 引用的 `01.5-rtvm-matrix.md`（显式路径优先，缺失时双落点 depth-1 扫描，多命中 fail-open SKIP）确定性派生 REQ/DES/TC 集合，以 `<!-- trace-derive begin/end -->` 标记块写入本变更 §4 节（幂等写前删旧块）；01.5 缺失/无 REQ 行/无 §4 节均 fail-open SKIP（G5/门禁 4 仍为执法层）；§4 模板补「自动派生优先、手写合法」措辞（派生块与手写行并存以派生块为准）；②**audit G5 扩第二断言（REQ-956）**——「01.5 REQ ⊆ 01-spec」（活文档目录内同名比对，缺即 FAIL 列行；01-spec 缺失豁免，对齐 G5 fail-open 阶梯）；③**加一删一预算（§5.2）登记**：新增 2 REQ，对等减法=§4 手抄四行形态降级为派生优先（手写保留作兜底，合并类）；④golden 406→419（T33 十三态：派生/幂等/缺矩阵 SKIP/单跑等价/--all 同章/REQ-only fail-open 阶梯/--all 派生等价比对），源层断言 355→359（A26 四 pin）+ 基线/README 声称数同步；⑤**独立评审四发现全采纳**：F1 pipefail 击穿 fail-open（三条赋值接 `\|\| true`）、F2 全文件删块跨节（删块节界化——awk 单趟，批内兄弟变更已交付块不再被扫）、F3 补测（REQ-only 矩阵 + 等价比对）、F4 G5 第二断言数字边界（`${r}([^0-9]\|$)`）；另修 explicit 路径解析真 bug（字类漏 CJK/全角冒号，单矩阵 fixture 掩盖） | **已接入项目**：`--upgrade` 带走 stamp-provenance/audit 两件 + 规范/AGENTS。**回填义务：无**——手写 §4 行保持合法；新变更 pre-commit 自动获得派生块。**规范条款级破坏面：无**（纯增能力 + 单向收紧的审计断言，01.5 与 01-spec 同源的仓库天然绿） |
| v3.41.0 | 2026-09-23 | **一键卸载（CHG-049，REQ-953，用户裁定"防误删优先"）**：新增 `scripts/uninstall-standards [--dry-run] [--force]`（随 `--guard` 安装）——**三层分类**：Tier1 安装器运行时件直接删（gate/stamp/session-gate/new-change/adapter/compliance/.agent-state/.git 活跃指针/hooksPath 仅当=配置的 githooks 目录，绝对路径等价形态同判；卸载器自身真跑后自删）；Tier2 安装器模板件**标记核验后删**（三钩子=agent-gate、PR 模板=五道门禁、三 workflow、.opencode 插件整件输出、entry=__CHANGE_ID__、project=独立完整声明/总册编号、bugs _templates=缺陷文档组六件套/BUG-xxx、docs/templates 兜底=安装器专属 token；被改动/无标记→保留并报告）；Tier3 **共享知识资产默认保留清单报告**（AGENTS.md/CLAUDE.md/规范/CHANGELOG/方法论/docs README/bugfix-log/总册/changes/bugs 实文档/.agent-governance.yml/合并型接线 .claude/.cursor/.gemini/**tests/audit+run-tests（run-tests 是用户追加 T 段的合并型扩展点，故意不进 Tier1）**），`--force` 也只**备份移动** `.uninstall-backup-<UTCts>/`（保留相对路径，整目录搬回即还原），永不 rm；`rm/mv` 失败 WARN 不虚报；汇总输出 removed/backup_moved/kept + 手工收尾指引（合并型接线剥离、README 徽标）；--dry-run 零副作用 | **已接入项目**：`--upgrade` 带走 uninstall-standards.sh+bootstrap 两件。**回填义务：无**（纯新增退出路径，不改任何既有门禁语义与产物要求）；重装即完整还原 Tier1/Tier2 清单 |
| v3.40.0 | 2026-09-22 | **执行成本第三批五快赢（CHG-044~048，FU-106 ①②⑤⑥⑧ 一次清账）**：①**pre-commit 溯源自动章（REQ-948）**——`pre-commit` 模板在 staged 校验前自动对活跃变更执行 `stamp-provenance.sh --all`（幂等，写前删旧块）并对其 `bug_ref` 绑定六件套逐一 `--bug`；无活跃变更/脚本缺失显式跳过（fail-open，gate staged 兜底）；AGENTS 门禁 3 手动盖章步骤删除、改自动+兜底措辞；②**P3 缺陷轻量通道（REQ-949）**——`01-diagnosis.md` 头部行首 `severity: P3`（文案类，精确形态）时 02-impact/05-config/06-tasks 允许缺省或单行「未命中，不适用（理由）」，01/03/04 恒必查，未声明/非 P3=六件全查（fail-closed）；gate 三处校验点（begin 循环/sweep standalone/sweep flat）共用 `bug_p3_lightweight`，扁平批次按成员小节判定 severity；③**红灯未清禁开新变更（REQ-950）**——begin 于水位门后新增红灯门：`.agent-state/session-gate-last.md` 含 `GATE RED` 且 mtime 新于最后交付 09-changelog → 拒（无报告/报告陈旧/无 changelog 基准 fail-open，对齐水位门阶梯；豁免 `AGENT_GUARD_ALLOW_OVER_RED=1` 并登记 09）；§2.9.6 换挡条 + AGENTS 门禁 2 同步；④**die 错误码标准化（REQ-951）**——agent-gate.sh 79 处 die 全部载 `GATE-Exx:` 前缀（顺序编号），new-change.sh 8 处 `NC-Exx:`、install-hook-adapter.sh 8 处 `ADAPTER-Exx:`；下游断言只认 `GATE-E\d+:` 形态不 pin 具体码；⑤**减法机制 G10 + 加一删一（REQ-952）**——通用层审计新增 **G10 废弃条款 sweep**（STD/AGENTS 内「废止」标记行必须同行带 `v3.x` 被替代指向，缺指向 FAIL 并列行；预算计数留政策层不机校，防 M2 过度设计）+ MAINTAINER 新增 **§5.2 加一删一预算**（每版本新增条款须对等淘汰或登记豁免理由；优先减法序：合并>下沉>删除>抬上界）；⑥golden 367→395（T27 自动章九态/T28 P3 六态/T29 红灯四态/T30 错误码四态/T31 G10 两态），源层断言 337→349（A25 +9 pin + v3.40.0 changelog 链），A11 体量实测重校（详见 MAINTAINER §5.1 下次参照） | **已接入项目**：`--upgrade` 带走 agent-gate/pre-commit/stamp-provenance/audit/session-gate/new-change/install-hook-adapter 七件 + 规范/AGENTS/MAINTAINER（§5.2）。**回填义务：无**——新条款只约束新变更；存量缺陷组未声明 severity = 六件全查（现行为）；**规范条款级破坏面**：pre-commit 升级后 commit 即自动盖章（首个 commit 前手动 `--all` 一次以过 staged）；die 前缀只增不改语义，下游 grep 兼容 |
| v3.39.0 | 2026-09-22 | **评审输入契约 + 会话遥测通用层（CHG-041/042，FU-106 第二批·用户 4 条反馈吸收）**：①**§2.2 评审/测试子代理输入契约（REQ-944）**——派发评审/测试子代理时主 Agent 必须随包供给证据（变更文件清单+diff 摘要、待核对产物路径、行号锚点），子代理超出清单的读盘动作须在结论声明原因（防悄悄重勘探——实测单个 Review 子代理 12.6m 重勘探根因即此）；`03.5-tasks.md` 增「评审输入」字段，begin A 层机校字段存在性（轻量通道豁免行不受影响）；②**§2.9.6 遥测通用层（REQ-945/946）**——轮次/工具计数逻辑从 v3.38.0 的 OpenCode 插件**下沉 `session-gate.sh count turn|count tool <name|->`（bash 通用层，harness 无关）**：Claude 适配器经 `UserPromptSubmit`/`PostToolUse` hook 转发、OpenCode 插件降为薄转发（`chat.message`/`message.part.updated` → bash 计数），其余 harness 接等价事件即可；计数落盘 `.agent-state/session-tool-stats.json` + `.agent-state/session-water.json`（gate begin 水位门文件格式不变）；**勘探预算机校（REQ-946）**：`session-gate idle/status` 读数单会话 `bash` >20 亮黄灯（软执法）要求 09 登记超标原因，AGENTS.md 纪律 1 量化、纪律 4 勘探下放改默认强制；§2.9.6 增交付前「换挡检查」登记项；③golden 359→367（T26 遥测九态：start 重置/turn 计数/工具分类/stdin tool_name/超限告警/静默下限/黄灯双面）；④源层断言 332→338（A25 调 3 pin + 新 5 pin），基线 331→337 | **已接入项目**：`--upgrade` 带走规范/AGENTS/agent-gate/session-gate/install-hook-adapter/run-tests 六件。**回填义务（一次性）**：存量变更的 `03.5-tasks.md` 在下一次 begin 前补一行「评审输入」（格式自由，含关键字即可；历史变更不回填）；重跑 install-hook-adapter 生成 v3.39.0 接线（Claude 增 2 hook、OpenCode 插件降为薄转发）。**规范条款级破坏面**：begin 对非豁免 03.5 新增「评审输入」存在性校验——存量仓升级后首个新变更需补该字段 |
| v3.38.0 | 2026-09-22 | **会话水位机校 + 变更入口脚手架 + metrics 产物体量（CHG-038/039/040，下游两轮外部审计结论"防漏→减税"落地第一批）**：①**§2.9.6 水位机校（REQ-941/942）**——OpenCode 适配器（install-hook-adapter 生成的插件）对 `chat.message` 计轮、会话开始清零、落盘 `.agent-state/session-water.json`、超限每 10 轮注入硬提醒；`agent-gate begin` 读磁盘水位文件超 `AGENT_GUARD_SESSION_TURN_LIMIT`（默认 50）拒开新变更，豁免 `AGENT_GUARD_ALLOW_OVER_WATER=1` 须 09「重要上下文」登记理由；gate 仍只读磁盘不依赖客户端运行时（§2.17 铁律不破），无水位文件客户端跳过检查（§2.17.3.2 防线后移语义不变）；session-gate status 增水位展示；阈值为拍定值，metrics 数据积累后复核；②**§2.17.1 入口件脚手架（REQ-939/940）**——`scripts/new-change <变更号> --risk Lx`（随 `--guard` 安装）从 `<docs>/templates/entry/`（`--core` 新发 begin 必检七件骨架模板）生成入口件，自动判定专用目录/当日批次归并（L0/L1 默认入批、L2/L3 一律专用、`AGENT_GUARD_ALLOW_INDEPENDENT=1` 豁免同 gate），治理 JSON 执行主体占位 `PENDING` 由 begin 占位校验拒绝——脚手架只生成骨架与登记不生成正文（§2.17.1.2 铁律），补齐"§2.17 派发仅覆盖 01 合入后、入口三件无一致起点"缺口；③**§2.17.5 metrics 产物体量（REQ-943）**——`artifact_files`/`artifact_bytes`（变更目录 `*.md` 件数与字节量，治理 JSON 不计同盖章豁免口径；批次内共享目录级数值跨成员不求和），为"规范瘦身/加一删一"预算提供数据；token 级成本（cache_read_per_turn/工具调用数）为平台运行时数据不进零依赖契约，需要时平台侧 adapter 另行导出；④golden 335→359（T24 水位四态 / T25 脚手架九态 / T7 体量字段）；⑤源层断言 320→332（A25 增 12 pin），基线 319→331 同步 | **已接入项目**：`--upgrade` 带走规范/AGENTS/STANDARDS_CHANGELOG/bootstrap/agent-gate/install-hook-adapter/session-gate/run-tests 七件 + `--core` 新增 `<docs>/templates/entry/` 7 件 + `--guard` 新增 `<scripts>/new-change` 一件。**回填义务：无**——水位门仅在装了适配器且插件跑过的会话产生水位文件后生效；脚手架为可选加速，旧入口流程继续合法。**升级后动作（可选）**：重跑 install-hook-adapter 生成 v3.38.0 插件（含计轮）；已装仓会话从下一次 session.created 起计轮 |
| v3.37.1 | 2026-09-21 | **FU-105 闭合：审计拦截功能目录表外 reviews/（CHG-037，用户指令"继续 105"）**：①`audit-docs-consistency.sh` **G9 面内新增 sweep**——功能目录（含 `01-spec.md` 或 `01.5-rtvm-matrix.md`，与 gate"看起来像功能目录"判据同源）内存在 `reviews/` 子目录即 FAIL 并点名，报文指引 §1.3 第 6 条唯一落点（条款全文见 v3.37.0 行 ③，不在此复述）；结构目录（changes/bugs/project/templates/methodologies/review）与隐藏目录豁免；功能目录集合为空时 `VACUOUS SKIP` 显式空转（防假绿）；只拦 `reviews/` 形态，不扩面；②golden 两例（表外判红+报文点名 feata；删除后复绿）+ 合规夹具回归；③audit-standards-src +2 pin（审计模板检查串 / golden 用例名），基线 317→319；④**存量组清单漂移修正（查漏补缺）**：SKILL.md"G1–G8 八组"→"G1–G9 九组"、双 README 审计行 G7 后补 G8/G9、AGENTS 锚点 G9 描述补"+表外 reviews/ 拦截"——三载体组清单与脚本头注释对齐 | **已接入项目**：`--upgrade` 带走 audit 模板一件即可（golden 随 tests 包）。**回填义务：无**——存量表外 reviews/ 由升级后首跑审计显式亮红，处置=删除/迁移至唯一落点或登记理由。规范条款零变化（REQ-936 明文本就存在），版本 patch 位 |
| v3.37.0 | 2026-09-21 | **总册命名分层 + P01 瘦身 + 评审落点明文 + 文档地图（CHG-036，下游接入项目返工实证四根因，用户裁定四项全做）**：①**12 总册文件名加 P 前缀**（`01-requirements-master.md`→`P01-requirements-master.md` 等 12+12 评审配对）——功能轨 `01-spec.md` 与总册同为 `NN-` 形态致"哪个 01 最新"反复歧义，分层信号落文件名（begin/G9/glob/评审配对公共面），gate/audit/run-tests/bootstrap/审计 A25 清单与 `P${f%%-*}` 推导同步（新名下推导改 `${f%%-*}`，防 `PPxx`）；②**P01 需求全景索引改统计+指针形态**（REQ-935）——模板原"REQ 编号/一句话描述/优先级/所属/状态"逐行必填表在 46+ REQ 下怂恿出第二份 01-spec（双处维护必漂移），改"统计行（总数/按功能分布/状态计数）+ 指针表（功能目录→01-spec/01.5 链接）"，明文**禁止逐 REQ 行展开与全文复制**，仅 REQ 集合变化时更新；模板/细节层/源仓实册三处同文；③**评审与产物落点唯一成文**（REQ-936）——§1.3 硬规则 5→6（五条→六条）+ project-masters §1 第 5 条：评审唯一落点=变更轨 07+总册 reviews/，**功能目录不建 reviews/ 等表外子目录**，新产物先查 §2.16.5 落点表、**表外默认禁止**（把下游"新文件先问能否并入"软规则升级为可对照落点表）；④**一页文档地图**（REQ-937）——新模板 `resources/templates/docs-readme.md` 由 bootstrap `--core` 安装为 `<docs>/README.md`（与 AGENTS.md 互补的人面地图：功能轨/变更轨/总册/缺陷组/治理件五层职责+更新时机+三硬规则；已存在同名文件且异内容时 CONFLICT 拒绝，fail-closed）；A25 增两断言（模板存在+bootstrap 收录）；⑤版本链 v3.37.0 三载体同步 | **已接入项目**：`--upgrade` 带走规范/AGENTS/project-masters/docs-readme 模板/bootstrap/audit/gate 六件即可。**回填义务（一次性）**：升级后下一变更 begin 前，把 `<docs>/project/` 12 册与 `reviews/` 12 份按 P 前缀改名（G9 显式红即回填清单）；P01 实册按统计+指针形态瘦身（删逐 REQ 行）；无 docs/README.md 的仓随升级获得。历史 CHG/BUG 产物不回填。源层断言 A25 随行更新 |
| v3.36.0 | 2026-09-20 | **缺陷批次扁平化（BUG-006，用户裁定"不需要建子文件夹，当天的在同一份文件上更新"，返修 v3.35.0/BUG-005 嵌套形态）**：①**§1.1/§2.5 阶段 6**——同日多缺陷默认共落 `docs/bugs/BATCH-YYYYMMDD/` **扁平**目录：六件套每份各一个文件、文件名不变，同批缺陷用 `## <BUG-xxx>` 小节锚点分开，**当天追加/回填落在同一套文件里**（与变更批次同构）；v3.35.0 嵌套子目录形态 `BATCH-YYYYMMDD/BUG-xxx/` 降为历史合法（存量不回改）；独立目录当日批次存在时被拒（不变）；②**gate 三形态统一解析**——`bug_group_dir` 增第三形态（批次内 `## <id>` 锚点；独立/嵌套/扁平多处命中 fail-closed die），`validate_bug_groups` 增扁平批次锚点存在性扫描（诊断件内每个锚点须六件各有同名小节，缺件 die、`.gate-allowlist` 豁免不变），`validate_bug_ref_provenance` 落批次目录后按六件校验；③**stamp**——`--bug` 增扁平解析，命中批次时块 `bug: <BATCH-id>` + `batch_changes:` 成员清单（与变更批次同构），risk 仍 n/a；④**audit**——G8 增扁平锚点缺件扫描、A21 增批次直属文件盖章扫描（嵌套子组由原循环覆盖）；⑤**模板两件**——`bugfix-log.md`「文档组落点」、`bug-impact.md`「关联缺陷」补扁平批次形态；⑥golden **T18d**（扁平锚点 begin 解析 / 锚点缺件 staged 红 / 补齐绿 / 嵌套历史回归）；⑦版本链 v3.36.0；README 声称数生成器同步 | **已接入项目**：`--upgrade` 带走 agent-gate + stamp-provenance + audit 三脚本与两模板即可。**回填义务：无**——v3.35.0 嵌套存量组保持原样即合法（历史不回改）；仅当某仓出现"仅 1 份合并诊断"的旧扁平日文件夹被新扫描判红时，按 §2.14 回填清单补六件锚点或登记 `.gate-allowlist`。源层断言 A25 锚点随行更新 |
| v3.35.0 | 2026-09-20 | **项目级总册体系 + 缺陷环节三条款（CHG-035 / BUG-005，用户裁定）**：①**§1.3 项目级总册（Project Master Set）**——`docs/project/` 12 册（P00 章程/P01 总需求/P02 总架构（含架构总图+关键时序总图）/P03 接口索引/P04 数据字典/P05 任务计划/P06 测试总册/P07 测试结论/P08 部署/P09 风险遗留/P10 变更总日志/P11 决策总册）+ `reviews/` 逐册评审记录（评审人≠册作者，追加式）；**每次变更强制回填**（09 §4 新增「项目总册回填清单」节，P00–P11 逐册 `[x]`/`未命中（理由）`，P10 无条件；gate stop/CI 逐册机校）；每册**独立完整**（自含正文禁止"仅见他册"，头部 `总册编号`+`独立完整声明` 自证行）；被更新章节留**变更注记**（`> 变更注记 CHG-xxx（日期）：摘要`，只增不删；原值可查：历史由变更目录快照+09「方案」承担，§2.15 规则 2）；**首次变更 begin 强制初始化**（豁免 `AGENT_GUARD_ALLOW_NO_PROJECT_MASTERS=1`+09 理由登记）；细节唯一权威层 `docs/methodologies/project-masters.md`，模板 `resources/templates/project/`（12 册+评审模板，`--core` 落 `<docs>/templates/project/`）；②**Bug 三条款**：诊断前必读总册相关册并在 01-diagnosis 引用（防只见代码不见设计）；修复落点不被既有 TC 覆盖**必须新增防回归 TC**（04+03-test-plan，禁止"修复无 TC/仅复跑不新增"）；**缺陷六件套按天入批**（`docs/bugs/BATCH-YYYYMMDD/BUG-xxx/`，废止 v3.22.0"不入批"；gate `bug_group_dir` 双形态解析+同日独立组拒绝+allowlist/ALLOW_INDEPENDENT 豁免；stamper `--bug` 批次嵌套解析；审计 G8/A21 双形态）；③门禁 4/DoD/§2.16 主线/§2.16.5 落点表/§3 清单/§2.15 更新语义表/§2.13.3 接口弃用/阶段 1/3/4/6/7/9 总册联动句同步；④audit 新增 **G9**（总册存在+自证+评审配对+最新变更回填清单）；源层审计新增 **A25**（23 项形态锚点），A11 上界 144KB→156KB 重校准（153,499 字节）；⑤`bootstrap --core` 扩发 methodologies(5)+templates/project(13)；⑥源仓 `docs/changes/CHG-034` 补录缺失的 5 件入口产物（A6e BUG-003 守卫红修复，重建件头注声明）；AGENTS/METHODOLOGY/PR 模板/pipeline 骨架/双 README/SKILL 版本三载体同步 | **已接入项目**：`--upgrade` 带走规范/METHODOLOGY/project-masters.md/agent-gate/stamp-provenance/audit/bug 六件套模板/**templates/project(13)**。**回填义务（下一变更 begin 前完成）**：①把 `<docs>/templates/project/` 复制为 `<docs>/project/` 并回填现状（从 README/既有 01-spec/代码现状合成，必含章节不可缺）+ 逐册初评 `reviews/`；②其后每个 CHG 的 09 按新模板加「项目总册回填清单」节；③`docs/bugs/` 存量独立 BUG 目录**合法保留**（双形态兼容），新缺陷按天入批；④审计 G9 失败项即总册补齐清单（§2.14）。源层断言 309，源层上界 156KB |
| v3.34.0 | 2026-09-18 | **四项打包：FU-042 收口 + 三提案落地（CHG-034，用户裁定"都做"）**：①**gate L0/L1 补"评审≠实现"机校（FU-042 闭环）**——validate_governance_state 最低线分支新增 implementation≠review_owner 比对（"评审独立"全等级机器闭环）+ golden 负例；②**metrics 专家会话数观测**——每变更 JSON 行新增 `expert_sessions`（01/03 署名节唯一任务 ID 去重计数）与 `expert_sessions_over_guardrail`（L2/L3 >3 超标标记）；**只观测不执法**（metrics 只读契约不变）；实测教训：emit_metrics 在 `set -euo pipefail` 下签名提取管道须 `|| true` 兜底（无署名仓曾全量 17 断言连锁红）；③**能力卡合规样例**——`expert-capabilities.md` §0.3 增「专家评审记录」golden 形状块（标题节+四列表格+§2.1.7 署名行+形状要点），下游照抄即过 A 层机校（方法论层承载，零新契约文件）；④**session-gate 规则 10 自查表注入**——start 路径读活跃变更治理记录，四主体表 + 互异判定写入 `.agent-state/session-gate-last.md`（冲突时 stdout 点名）；执法点前移到会话开始（防跑偏非防事后），软执法不阻断（start 永不 exit 非 0）；⑤版本链 v3.34.0；golden 308→310；README 声称数生成器同步 | **已接入项目**：`--upgrade` 带走 agent-gate + session-gate.sh + expert-capabilities.md 三件。**回填义务：无**。源层断言 283 不变 |
| v3.33.0 | 2026-09-18 | **规则 10/两批制 A 层执法（CHG-033，FU-039+FU-041 合并落地）**：①**agent-gate `validate_governance_state`**——全等级必填 `spec_author`（非占位）；有 review_owner 时 `spec_author ≠ review_owner`（最低线）；L2/L3 四主体（spec_author/implementation/test/review）互异，报文指向 §2.1 rule 10；②**agent-gate `validate_delivery`（stop/CI）**——`01-spec.md`/`03-modification-plan.md` 中**标题形态**的「专家评审记录」节必须含 §2.1.7 三段式署名（正则容忍 `/ ` 两侧空格——首版实教训：§2.1.7 规范格式 `Claude Code / model / task` 带空格，严格无空格正则把合规签名判红），无该节=合法空转（L0/L1 轻量通道），正文行内提及不触发（标题锚定，防 FU-014 同族假红）；③golden 新增 **T22**（-7+：署名缺失拦/带签名过/行内提及不触发/spec_author=review_owner L0 拒/缺 spec_author 报文/L2 重复拒），存量 17 处夹具治理记录批量补 `spec_author`（MAINTAINER §2 行5 预警的"夹具集体失效"如期发生并修复）；④**golden 假绿防御**：T22 正例最初把 `stamp_fixture_all` 退出码当 stop 退出码（`report ... $?`），改 `out=$(...)`/`rc=$?` 捕获——夹具自己的 rc 债，顺带清偿；⑤规范措辞"待落地"→"v3.33.0 落地"（§2.2/§5）；FU-039/041 闭环；⑥版本链 v3.33.0；README 声称数 299→308（生成器同步；评审收口 +2） | **已接入项目**：`--upgrade` 带走 agent-gate 即可。**行为变化（唯一）**：升级后新变更 `00-governance.json` 必须声明 `spec_author`（模板 v3.31.0 已含字段）；老记录仅在被重新用作活跃变更时需补。**回填义务：无**。源层断言 283 不变；golden 299→308 |
| v3.32.0 | 2026-09-18 | **专家两批制 + 第三轮 review 修复（CHG-032；用户指令条款化："调研/分析的需求专家与评审需求的专家必须不同主体，方案设计专家与方案评审专家必须不同主体"）**：①**§2.2 新增「专家两批制（生产-评审分离）」**——每阶段专家分生产批（产出阶段产物）与评审批（≥1 名未参与生产的专家 + Review Agent 签署），**专家不得评审自己参与生产的产物**；典型分派入条款（需求=业务/行业生产→Review+未参与行业/业务评审；设计=技术/架构生产→PM+Review 评审）；L0/L1 最低线=作者≠评审（规则 10 全等级）；②**规则 10 补锚**——`spec_author` 填 01-spec 作者（需求 Agent）标识，03 方案作者核对走签署表"方案评审"行；生产批/评批互引；③**§2.16.3 DoD 门禁 5 行补核对项**（作者≠评审全等级/四主体互异 L2+）；④第三轮 review 修复——§2.2 FU-038→FU-039 陈旧编号、§5 代签行"（v3.30）"判定句改"待落地"（gate 无署名校验，如实）、§3"五阶段矩阵"→"七行阶段矩阵"、§5 两行压缩（-190B）、L0 行括注减冗；⑤**CHG-029 未落盘缺陷补正**——`agent-governance.yml`:21 与 `stamp-provenance.sh`:16 计数注释 12→15（v3.29.1 曾声称已改，磁盘未改——编辑未落盘类缺陷，本版修复并留痕）；⑥`resources/AGENTS.md`——门禁 1 补 spec_author、锚点表 +规则 10 行 + 能力卡文件指针；`bootstrap.sh` --help 补 "methodologies/ (4)"；⑦**出处索引补缺**——最小权限/纵深防御（Saltzer & Schroeder 1975）成行；待核行并入 Prompt A/B 与 Release Readiness Review；⑧版本链 v3.32.0；FU-041 登记（gate 硬校验 spec_author≠review_owner，与 FU-039 同变更承载） | **已接入项目**：`--upgrade` 带走（spec+AGENTS+governance.yml+stamp 注释四件）。**回填义务：无**。源层断言 282、golden 299 不变（A11 余量充足未重校准） |
| v3.31.0 | 2026-09-18 | **理论出处索引 + 角色单一职责（CHG-031；用户两条强指令：FU-040 补出处、"写文档/评审/执行不得同一主体"）**：①**`expert-capabilities.md` 补「附：理论出处索引」**（25 行三列表：理论/提出者/载体——PMBOK、STRIDE、HTSM、深模块、C4、SLO 等注名理论全部可溯源；多源实践并入表尾"（待核）"行不编造；内容由独立文档作者子代理主笔——本变更即角色分离演示）；②**§2.1 新增规则 10「角色单一职责（一人一角色）」**——同一 CHG 内作者/实现/测试/评审四主体 §2.1.7 标识互异（L0/L1 最低线＝评审独立且作者≠评审，测试按 §1.1 合并留痕）；编排 Agent 不得兼任任一签署；同标识无豁免＝门禁 5 不通过；③**`governance-state.json` 模板增 `spec_author` 字段**（增量，json_field 按名读取，门禁零改动；历史记录不回填）；④§5 反模式行"一人多角色"=🔴 严重违规；`resources/AGENTS.md` 角色段补"同一变更内不得兼任多角色"；⑤版本链 v3.31.0；A11 重校准 140→144 KB（规则 10+§5 行超余量 220B，先压缩两处措辞仍压线，KiB 步进） | **已接入项目**：`--upgrade` 带走（spec+AGENTS+governance-state.json 模板三件）。**回填义务：无**——`spec_author` 只约束新变更，历史 JSON 无该字段属合法空转。源层断言 282 不变（A11 仅阈值）、golden 299 不变 |
| v3.30.0 | 2026-09-18 | **专家能力卡与方法论自适应（CHG-030；用户裁定"专家有深度/广度/理论/项目经验，每次出动=质量提升时刻"）**：①新增方法论层文件 **`methodologies/expert-capabilities.md`**（随 `--core` 下发）——**9 核心 + 3 条件命中专家**各一卡（理论工具箱〔基线 M0 / 扩展 M1-M2，注名经典方法论：PMBOK/关键链、STRIDE/OWASP ASVS、USE/RED、HTSM/测试金字塔、深模块/C4/ADR、用户故事地图/Impact Mapping/Kano、ATDD/SLO、数据迁移双写/对账、评估集/Prompt 版本化 等，注名不搬运〕/ 广度协同 / 经验装载 / 自适应锚点）；条件命中：数据专家（§2.6）、LLM 专家（§2.9）、可靠性专家（§2.7/§2.8）——命中才出动，不占 L2 ≤3 会话护栏；②**§2.2 新增强制条款**——能力卡指针（组合表不变）+ **方法论自适应绑定**（M 层判定 + AGENTS.md「项目速览·方法论偏好」行自适应，动用扩展须评审记录注名）+ **经验装载**（bugfix-log 同族 + FU 台账必核，重复踩坑=评审无效）+ **增量承诺**（每次出动 ≥1 发现或显式达标确认 + ≥1 可执行改进建议；零发现零建议=泛评退回）+ **能力回填**（§2.14/阶段 10）+ **专家扩展协议**（项目特定专家经项目速览声明，四维卡同构，不改 §2.2 组合表）；③**载荷接线**——`bootstrap.sh` 复制清单 +expert-capabilities.md（audit methodologies 遍历为动态 `ls` 自动纳新）、`SKILL.md` 核心层清单与项目速览提示（+方法论偏好/专家扩展行）、双 README 树与叙事 +1、MAINTAINER 角色表按目录已覆盖；④版本链 v3.30.0 | **已接入项目**：`--upgrade` 一次带走（spec+bootstrap 清单+新文件）。**回填义务：无**——能力卡只约束后续专家出动；未声明偏好的仓自动用基线工具箱，零行为差异。源层断言 279→282（methodologies 动态遍历 +新文件页脚与聚合断言，生成器重写基线）、golden 299 不变 |
| v3.29.1 | 2026-09-18 | **全项目对齐与压缩续修（CHG-029，循 v3.21.1 先例；三路独立评审 22 项必修全采纳，零规范性条款变更、无行为变更、门禁/golden 零改动）**：①计数修正——"变更 12 件产物名"实为 **15 件**（00-intent/00-governance.json/01/01.5/02/03/03.5/04/04.5/05/06/06.5/07/08/09；§1.1 ×2、SKILL ×2、双 README、templates 注释 ×2 同步，golden 无锚点已核）；②§2.2 与 §0.5.2 冲突消解——bugfix 句改"其角色合并按 §0.5.2 判定天然放宽，非本表另立豁免"；③**专家评审执行钩子补齐**——§3 清单新增「专家评审（§2.2）」勾选项、§2.16.2 主线阶段 1/3/5/8 补钩子、§2.5 阶段 8 必含清单补「整体验收」节、§4 签署表前补"专家签署落「专家评审记录」不入本表"、§5 新增反模式行（泛评无证据/开发代签，🟡，FU-039）；④§2.2 上下文契约引用修正（§2.16.1 第 1/3/4 步，02 行业合规为跨阶段迭代输入）+ 表头注"按 PMP 过程组序排布"；⑤跨文件衔接——`resources/AGENTS.md` 角色段补 §2.2 衔接句、METHODOLOGY 双视角行对齐九专家、development.md 补安全/性能专家指针、SKILL 核心提法补"分阶段专家评审"；⑥双 README——Features 补 2 行（专家评审/会话内执法）+ 事件表补会话行 + zh "v3.24.0/v3.29.0" 漂移修正回 v3.24.0/v3.25.0 + twelve→fifteen + "永久生效"改务实措辞；⑦CHANGELOG 版本链断档补齐（[3.28.0]/[3.29.0] 节 + compare 链，重复"### 新增"合并，断言数硬编码 292 删除）；⑧MAINTAINER——§5.1 上界句 128 KiB→140 KB、"最近一次重校准"补 v3.25/v3.29 两行、CHG-001..016→028、v3.21.2 "6 条"→10 条、§2 联动表补方法论层页脚同步；⑨**压缩**——§2.2 测试/四方行改引用式（判定权威归 §2.5）、漏项检测去重复述、SKILL G1–G8 枚举收敛为指针（保 audit-docs-consistency 锚点字面量）、AGENTS.md 红线管道过滤改指针；⑩**废止"双 README ≤200 行"目标**（v3.22.0 重写后失效，本行即裁定登记） | **已接入项目**：`--upgrade` 带走即可（spec+AGENTS+METHODOLOGY 三件）。**回填义务：无**——全部为对齐与表达修正，既有产物原样合规。源层断言 280、golden 299 不变 |
| v3.29.0 | 2026-09-18 | **分阶段专家子 Agent 评审矩阵（§2.2 新节；CHG-028，用户裁定对齐 PMP 最佳实践，两轮评审 12+4 条发现全采纳）**：①**§2.1 角色表 +9 专家行**（业务/行业/技术/架构/项目管理/安全/性能/测试/整体验收专家）——专家＝既有角色**细分执行主体**（括注映射：业务＝需求 Agent、技术/架构＝架构-计划 Agent、项目管理＝编排调度面+拆解面、测试＝测试 Agent、整体验收＝Review 终审扩展+Release Owner 人类面），**签署权一律映射回 §0.5.2 既有角色列**，不另立权威源；②**§2.2 新节**——五阶段×（专家组合/评审动作/产出落点）矩阵：需求四步循环（收集→分析→验证→评估，PMP 收集需求/定义范围/确认范围前置）、设计 WBS（架构+项目管理）、开发任务监控与**漏项检测**（DoD/DoT 逐项、RTVM 未映射清零、防"大部分完成即标完成"）、测试 QC 面＋**NFR 反向核对**（01 非功能清单逐条↔TC 映射，双向防漏）、交付**整体验收四方证据**（代码/交付物/配置部署/文档，任一方退回即整体退回）；③**上下文贴近契约（防泛泛而谈）**——专家就位前必须加载项目上下文（AGENTS.md 项目速览/02 行业合规/RTVM/bugfix 教训＝§2.16.1 第 4 步），每条结论须引用 **≥1 条项目具体证据**（路径:行号/技术特征/行业约束/REQ 编号），**纯模板泛评＝B 层退回（等同未评审）**；④**行业专家二分触发**——02 命中合规敏感域（金融/医疗/数据出境）或 L2+ 强制独立主体，通用 L0/L1 业务专家兼任留痕；⑤**署名与代签禁令**——评审记录须对应专家子 Agent 署名（§2.1.7），开发主体禁代签（A 层署名机校登记 FU-039 留 v3.30）；⑥**成本护栏**——同阶段双专家可合并单子 Agent 双视角会话（循阶段 4 先例）、L2 每变更专家评审会话 ≤3、记录单行/条并入 §2.5 ≤40 行预算；⑦**bugfix 豁免**——阶段 6/§2.11 走六件套复用测试/验收视角；⑧§0.5.2 补"专家独立性归本表唯一权威"注；§2.5 阶段 1/2/3/4/5/8 B 层验收行补专家责任句；`resources/AGENTS.md` 锚点表补 §2.2 行；**门禁/golden 零改动**（A 层判定强度、产物文件名、目录布局逐条不变）；⑨源层审计 **A11 上界 132KB→140KB 重校准**（KiB 步进协议；v3.28.0 余量实测 3 字节，§2.1/§2.2 专家条款演进承载，本行即依据） | **已接入项目**：`--upgrade` 带走即可（spec+AGENTS 两件）。**回填义务：无**——新条款只约束新变更；存量产物不含「专家评审记录」节属合法空转（不回填、不追溯）。源层断言数 279 不变（A11 仅阈值参数）；golden 299 不变 |
| v3.28.0 | 2026-09-17 | **会话内执法 + 六件套/溯源执法闭环（8 项；根因复盘：rainy 仓 CHG-065~071 / BUG-040~055 全链违规——执法链锚在 commit 而产物长期不提交、gate `json_field` 对数组 `bug_ref` fail-open、六件套校验"刻意排除"出审计面后委托给一个从不运行的门禁）**：①**gate `json_field_values`**（agent-gate）——`bug_ref` 扁平字符串与扁平字符串数组都解析；**声明但不可解析即 die（fail-closed）**；BSD sed 兼容用 `[^]]`（macOS 上 `[^\]]` 恒空是本 bug 的机械根因）；②**gate 六件套目录级扫描 `validate_bug_groups`**——staged/stop/CI 逐组校验 `<bugs_root>/BUG-*/` 六件齐，豁免 = `<bugs_root>/.gate-allowlist` 登记理由（登记组 WARN 点名不隐藏）；stop 另校验本变更 `bug_ref` 绑定组全部六件的溯源块；③**会话内执法 `session-gate.sh`**——会话开始跑 audit（`--only-fail`）把存量红灯写 `.agent-state/session-gate-last.md` 并注入会话上下文/结构化日志；会话空闲跑 `agent-gate --stage stop` 等价检查，无活跃变更但有源码改动 = 红灯；④**`install-hook-adapter.sh` 自适应重写**——探测（env 标记 / CLI on PATH / 仓库配置目录三路证据）→ 按客户端 schema 生成（claude=JSON 合并幂等去重 / opencode=`.opencode/plugins/dev-standards-gate.js`（session.created→start，session.idle→idle）+ `/gate-check` 命令 / cursor、gemini=静态 schema diff+force / codex=如实声明由 Git hooks+CI 兜底）→ **生成后强制验证**（JSON 可解析 / 插件 bun|node 可加载 / hook 脚本 `bash -n`），验证不过即安装失败；`--detect` 打印矩阵，`--client` 显式指定；**不写死任何客户端**；⑤**审计接管六件套**（audit-docs-consistency.sh）——删"刻意排除"委托，新增 **G8 六件套存在性**（allowlist 感知）+ **A20 变更目录盖章互证**（同目录半途盖章 = 红；`standard_version >= v3.26.0` 整目录未盖 = 红；不触发历史回填禁令）+ **A21 六件套盖章**；⑥**`stamp-provenance.sh --bug <BUG-id>`**——六件套盖章（块 `bug:` 标注、`risk: n/a`），溯源强制范围扩到缺陷组（§1.1）；⑦六件套模板各加溯源验收项、bug-diagnosis 元信息补「诊断人/处置主体」行（内容层归属照常手写，环境级真值仍禁手写）；⑧`is_code_path` 排除 `.opencode/` `.agent-state/` 与 `session-gate.sh`（防 opencode 用户首次 commit 死锁）；⑨（全局 review 补）`AGENT_GUARD_SKIP_VERIFY=1`——session-gate idle 的 stop 等价检查跳过 `verification_command` 全量回归（session.idle 可能每轮触发，`mvn test` 级命令跑不起；真实交付线不设此变量、全强度不变）；session-gate 的 active-change 路径改为与 gate 同源（`git rev-parse --git-path agent-governance/active-change`） | **已接入项目**：`--upgrade` 带走（agent-gate / audit / stamp / session-gate.sh / install-hook-adapter 五件）。**回填义务**：既有缺陷组跑 `--bug <BUG-id>` 补盖章（写入当下真值，用户裁定不属伪造）；六件套缺件按 G8 红灯逐组回填或登记 allowlist；既有变更目录存在"半途盖章"的（如 CHG-071 2/9）补跑 `--all`。断言/golden：299（golden）/280（源层）总数不变——T20 ⑦ 空转 grep 收紧为精确组名 `G3/G5/G6 VACUOUS SKIP`，避免 G8/A20/A21 合法空转声明被误判；数组解析/allowlist/半途盖章/插件加载经沙箱 e2e 验证 |
| v3.27.0 | 2026-09-17 | **同日批次默认强制（begin fail-closed）**（用户实测：下游仓同日 5 组 L0/L1 全部各开独立目录未合批——v3.24.0 默认化但 begin 不拦，默认零强制即第三例"减速带没人踩"；L2/L3 独立合规不受影响）：①**gate begin 新增判定**——当日 `BATCH-YYYYMMDD` 已存在时，L0/L1 变更必须入批，独立目录 begin 拒绝（报文含批次路径与豁免方式）；豁免 = 显式 `AGENT_GUARD_ALLOW_INDEPENDENT=1` 且理由登记 09（fail-closed，非静默旁路）；单变更日（无批次）与 L2/L3 独立权利不变；②**§5 反模式表新增**"同日多个 L0/L1 各开独立目录"= 🟡 中度违规（同族根因 CHG-023 ③）；**§1.1 补 begin 强制句**；AGENTS.md 第 1 步锐化；③golden T18b +6 断言（拒绝/报文/豁免/入批/L2 放行/（复用）），README 声称数 generator 同步；④下游存量处置（rainy 仓 CHG-066~070 是否回溯合并 BATCH-20260917）**待用户裁定**，不在本版执行 | **已接入项目**：`--upgrade` 带走 agent-gate 即可（spec/AGENTS 两处措辞随行）。**回填义务**：无。断言总数 280（源层）不变；golden 294→299 |
| v3.26.0 | 2026-09-17 | **溯源全产物强制 + BUG/CHG 不双建**（用户全项目 review 实证：下游仓 55 个变更目录、每目录 9 件 md 仅 04.5 有溯源块——v3.22.0 把 `--all` 定为可选、gate 只校验 04.5，要求落在可选层即被跳过；同仓 changes/BUG-045 仅 1 件诊断文件——BUG/CHG 双建边界无明文）：①**溯源强制范围 = 变更目录全部 `*.md`**（`00-governance.json` 刻意豁免不变，历史不回填不变）——`stamp-provenance.sh --all` 从可选升为**交付必跑**，gate stop/CI 在 validate_delivery 末尾逐文件校验、缺一件即不可交付、报文点名文件（校验移至末尾：06.5/06-delivery 等结构类失败仍先报自身检查项，报文语义零变化）；②**BUG 与 CHG 不双建（§2.5 阶段 6 新条款）**——缺陷默认只落六件套 + bugfix-log 双登记，禁止同时新建 CHG/变更目录承载同一缺陷；升级变更轨三条件（需求/行为变化需 REQ-RTVM 链 / 跨系统契约结构变更 / L2/L3 需独立签署），升级时目录号 `BUG-xxx`、`bug_ref` 指向六件套、诊断正文以六件套为唯一权威只引用不复制；③**§1.1 补句**：缺陷的变更轨入口 `BUG-xxx`（L0/L1）同样适用按天合并（六件套依旧不入批）——回答"同日 CHG/Bug 按天合并"：v3.18.0 已实现、v3.24.0 起默认化，本版仅补适用面明文；④golden 同步：夹具全量内联盖章 + 新增 "stop rejects any unstamped artifact" 负例（断言 292→294）；⑤术语表/§2.16.3/阶段 5 验收行/AGENTS 第 3 步/stamp 头部注释五处措辞联动 | **已接入项目**：`--upgrade` 带走（agent-gate + stamp-provenance + spec 三件）；升级前老 gate 不校验全产物（优雅降级）。**回填义务**：无——历史产物不得回填规则不变；**今日未交付的活跃变更目录应补跑 `--all`**（补的是活跃变更，合法）。断言总数 280（源层）不变、golden 294 |
| v3.25.0 | 2026-09-17 | **执行侧纪律二批（写侧并行 + 产物最小表达形态）**（承接 v3.24.0 同源平台规则 v2 与管线 token 实测：cache_read = 水位 × 轮数；3 变更 × 10 产物逐轮写 ≈ 6M cache_read；门禁判定强度、产物文件名、目录布局零改动）：①**AGENTS.md 执行资源纪律五条 → 八条**——+并行批量写（写产物/独立编辑单消息并行，仅内容依赖才拆轮）、+定向验证优先（模块/单测级先行，全量回归仅由门禁 stop/staged/CI 触发，禁随手裸跑全量）、+有状态 mock 跨调用隔离（"第二次调用被跳过"类失败先查共享 mock 内部状态）；纪律 1 补通用过滤模板形态 `<验证命令> 2>&1 \| grep -E "<FAIL/ERROR 汇总词>" \| tail -N`；②**§2.5 表达成本条款追加「产物最小表达形态」**——00-intent 3-5 行摘要 + "详见 02"（gate 硬校验节保留）、03 候选对比每案 ≤2 行（保 DES+选型/备选关键字，A 层兼容）、03.5 免拆单行声明+里程碑两行、验收框先 [ ] 实测后一次置 [x]、超行典型承载=02/04；形态裁剪不改变任何 gate A 层结构标记要求；③**§2.9.6 追加定向验证优先 + cache_read 量纲注**；④**A11 上界重校准 128KB→132KB**（v3.21.2 先例：先压缩、有 CHANGELOG 条目、KiB 步进；v3.24.0 收口余量仅 3 字节，上界已失效）；曾提案"01-spec 只含 REQ 表+可断言验收"与 v3.23.0 轻量通道"01 不可占位"+§2.5 阶段 1 必含六项冲突，**未采纳**，登记 FU-037 待用户裁定；DB DML 禁令/观测日志取证四步/字段提取模板/前端事件链等项目方言不入规范；⑤**整体一致性对齐**——双 README 治理细节补执行侧 token 纪律 bullet、批次措辞"可共用/may share"→"默认/default"同步、SKILL.md 批次默认化、术语速查表 +3 行导航、FU 台账登记 FU-036~038（含 FU 编号碰撞返修：登记前必须核对台账最大编号） | **已接入项目**：`--upgrade` 带走即可。**回填义务**：无——表达成本条款，既有产物原样合规。**已接入项目须同步**：本版触碰 `tests/audit-standards-src.sh`（A11 阈值，源层脚本不随 Skill 下发，目标仓零影响）；golden/断言数 280+292 不变 |
| v3.24.0 | 2026-09-17 | **执行侧 token 纪律入规范**（承接 v3.23.0 同一 714M 事故的执行侧 ~65% 归因，条款自平台级用户规则提炼为跨平台规范；门禁判定强度、产物文件名、目录布局零改动）：①**AGENTS.md 新增「Agent 执行资源纪律」五条**（红线追加"长输出必须管道过滤"）——管道过滤/一次 grep 合并拉全/先 `grep -n` 定位后 ±80 行小窗读/勘探类（>2 文件或 >100 行）委派只读子代理/无匹配≠通过（退出码与 FAIL 汇总为准）；措辞全泛化，mvn 等项目方言不入规范；②**新增 §2.9.6 会话与上下文纪律**——单会话单主题（禁混 bugfix 与 CHG）、>50 轮或主题切换或上下文长期高位即 handoff（§2.16.1 磁盘权威重建）、明知超标续用按 §5 登记；③**§1.1 批次默认化**——同日多个 L0/L1 由"可共用"升"默认共用"（独立目录须有理由并登记），L2/L3 不入批/解析顺序/闭环语义/审计 G7 逐字不变，术语表与 §2.16.5 落点总表两处引用同步；④**§2.5 总则新增表达成本条款**——跨产物引用制（根因/影响以 02 为唯一权威，00-intent/09 一行索引引用，与 §4 短引用制同型）+ 单产物默认 ≤40 行**软上限**（05/09 累积型豁免，超行头部一行说明理由；只裁表达冗余不裁必含清单规格下限）；曾提案"01-spec 只列 REQ+验收"经核对与阶段 1 必含清单六项冲突，**不采纳**（00-intent 开放问题留痕） | **已接入项目**：`--upgrade` 带走即可。**回填义务**：无——四条均为执行纪律与表达成本条款，既有产物保持原样即合规；新变更按批次默认化执行。gate/audit/golden 零改动（新增文本无断言依赖，断言总数 280、基线 279 不变；golden T21 夹具版本字面量改版本无关形态，断言数/语义不变） |
| v3.23.0 | 2026-09-16 | **Token 成本治理四条**（下游接入仓实测单会话 714M 超标，框架侧归因四项；门禁判定强度、产物文件名、目录布局不变）：①**验收勾选改条目号引用制**——§2.16.6 第 1 条"逐项复述强制"改"逐项引用核对"（条目号 + 勾选 + 一句结论；A 层附命令与**过滤后关键输出行**，B 层独立签署），**不要求抄录清单原文**（上下文与产物双份 verbatim 复制显式定为反模式），§2.5 总则 / §2.16.2 / §3 清单 / §5 反模式 / `resources/AGENTS.md` 步骤 4 五处联动，"只写已验收=未完成"拦截语义逐条保留；②**审计 `--only-fail`**——`tests/audit-docs-consistency.sh` 新增 flag：只打 FAIL 行、skip/VACUOUS SKIP 行（v3.21.0 空转声明豁免抑制）与汇总，exit code 不变，默认路径逐字节不变；③**L0/L1 轻量通道**（§1.1 新条款 + §0.5 指引 + §5 中度违规行）——02/03/03.5/04 四件允许一行"未命中，不适用（理由）"显式声明满足存在性（与门禁 A 层结构标记形状兼容，gate 零改动），01/04.5/05/09/RTVM 不可占位，L2/L3 不适用，文件名与布局零改动；反向违规同表登记（L3 仪式套 L0/L1 = 中度违规）；④**Observation 输出瘦身**（§2.16.2 + §4 模板）——归档"过滤后关键输出（建议 ≤10 行）+ 完整日志落点文件"，禁止整段原始输出贴入对话或产物；全规范 9 处"实际输出"措辞统一为"关键输出"（快速参考表/流程图/§2.5 A 层总则/ReAct 微循环/§2.16.3 DoD/§3 清单/§4 表头/§5 A 层行/`resources/AGENTS.md` 红线），消除"定义处瘦身、引用处仍要实际输出"的自相矛盾；⑤**09 双节引用制**——§4「追踪矩阵映射」加"短引用 ≠ 复制"块（一行索引指向 01.5，禁止复制矩阵行，01.5 为唯一权威）；「执行记录（ReAct）」表行允许"见 05 批次 N"引用（前提 05 已有对应记录，重复粘贴视为冗余）——G6 节存在性 / gate 4 只认 01.5 成行，机器校验语义零变化。同批曾提案"规范加载分层化"（AGENTS.md 步骤 1 改按需读取），**用户裁定撤回**，全文强制维持 | **已接入项目**：`--upgrade` 带走即可。**回填义务**：无——四条均为表达成本条款，既有产物保持原样即合规；新变更按轻量通道执行（L0/L1 的 02/03/03.5/04 可单行声明，抄录式清单不必回改）。`--only-fail` 为可选 flag，CI 无需改动 |
| v3.22.0 | 2026-09-16 | **接入动线与溯源覆盖收尾**（用户六项全项目 review：①溯源覆盖 ②按天批次 ③自动化审计 ④一句话克隆安装 ⑤免手动叙事 ⑥README 规范化；**不含新规范条款**，门禁判定强度、产物名、目录布局逐条不变）：①`scripts/stamp-provenance.sh` 新增 **`--all` 全量盖章**——一条命令给活跃变更目录全部 `*.md` 产物盖溯源块（复用既有 `resolve_dir`/幂等块替换，批感知不变；`00-governance.json` **刻意不盖**——JSON 注入 HTML 注释破坏门禁/审计的扁平 JSON 读取）；**强制范围不变**（§1.1 仍只强制 04.5），默认（无 `--all`）行为逐字节等价；②**源层自锁修复**——`tests/audit-standards-src.sh` A19 反向断言由"历史变更目录一律不得盖章"修正为"**溯源块 ⊆ {活跃变更} ∪ {已闭合变更}**"：原语义与 §1.1"交付前必须盖章"直接矛盾（本仓 17 个变更 0 盖章、一盖即红，溯源条款在源仓从未走通过）；仍拦截对无关开放目录的随手盖章（真正的伪造入口），已闭合目录的块是交付时的合法产物；③新增 **`scripts/install.sh` 一键安装器**（源层，同 bootstrap.sh 不随 Skill 分发）——`curl -fsSL …/install.sh \| bash -s -- <目标仓库>` 一条命令完成"克隆 Skill + 全量安装"，目标仓已接入自动转升级（`--upgrade`/`--install` 可显式指定）；薄委托设计：安装/冲突/脏树语义 100% 复用 bootstrap（不复制清单，防双权威源漂移）；`--from <path>` 离线路径（测试/内网）、`--skill-dir`/`--repo`/`--ref`、层与目录根 pass-through；4) **双 README 规范化重写**（章节化 About/Why/Features/…/FAQ；全部截图保留；自动化叙事改为"命令由 Agent/Hook 自动调用，手册仅供查阅，手动可选"）；逐版本说明迁出至新建**项目级 `CHANGELOG.md`**（Keep-a-Changelog；与 `STANDARDS_CHANGELOG.md` 分工：后者只记规范条款）；5) FAQ 落"按天批次"问答：变更轨 BATCH-YYYYMMDD 已实现（v3.18.0），**缺陷六件套刻意不入批**（逐缺陷证据边界；bugfix-log.md 索引即天级检索；缺陷的变更轨入口 `BUG-*` 仍可入批）；6) golden 新增 **T17b**（--all 全盖/JSON 排除/幂等/零成功即失败）与 **T21**（install.sh 离线安装/幂等/自动升级/冲突 fail-closed，最小 fake 源仓零网络）；源层审计新增 **A23 ×15** 形态锚点；断言生成器同步（README 声称数 + A10 基线） | **已接入项目**：`--upgrade` 一次带过去即可（`resources/templates/stamp-provenance.sh` 为唯一载荷变更），**无回填义务**——`--all` 是可选加强，不跑它行为与升级前一致。**可观测差异**：源仓 clones 多出 `scripts/install.sh`（源层，不进目标仓）；双 README 章节结构变化（书签可能失效，内容语义不变） |
| v3.21.2 | 2026-09-16 | **独立复核发现的对齐续修**（同日第二轮，承接 v3.21.1；**仍不含新规范条款**、无行为变更；共 **10 条** = 9 条对齐修正 + 1 条 A11 上界重校准，其中 ⑦⑧⑨ 为**第二路对抗性复核**追加）。①`resources/DEVELOPMENT_STANDARDS.md`「Agent 自进化与派生资产」节**删去两个派生 Skill 名举例**——该文件随 `--core` 下发到**每一个**目标仓库，在里面留派生资产的名字与本节自己的判定口径冲突（概念保留、名字不留）；②同文件 §2.17 的 `01.5-rtvm-matrix.md` 边界句校正：原文"`agent-gate` 的 `validate_delivery` **不校验该文件**"改为准确表述——它**不把 01.5 当作本变更的必检产物**（不校验其存在与节完整性），但**会读取任意 01.5 做「门禁 4」RTVM 回填核对**（`09-changelog.md` 引用的每个 `REQ-` 必须在某份 01.5 中成行）；原文与本段自身的"门禁 4 机器兜底（§2.13.4）"互相矛盾；③`SKILL.md` 的路径根说明补上 v3.21.1 遗漏的条件——`change_root`/`bugs_root` 随 `--docs-dir` 派生**仅当现值仍是内置默认**（`docs/changes`/`docs/bugs`），显式 pin（如 `specs/changes`）不被静默覆盖（实现已在 v3.21.1 修正，面向 Agent 的执行入口未同步）；④`resources/templates/agent-governance.yml` 的 `required_before_merge` 与门禁实检集对齐：**3 件 → 5 件**（补 `06.5-deployment-config.md`、`06-delivery-summary.md`），并注明三项内容校验（溯源块 / ReAct `Observation` / 门禁 4 RTVM）；该清单与 `required_before_source_write`（7 件 = `begin` 实检集）同为"团队可复核摘要"，此前 merge 侧窄于 `validate_delivery`；⑤`scripts/bootstrap.sh` 的 `change_root`/`bugs_root` 派生改**三态判定**——新增"值已等于本 docs 根派生出的值"一态（安装期 `transform_src` 已把模板改写成该值），消除全新 `--all --docs-dir <root>` 安装时打印 **"…an explicit pin wins…" 假 NOTE** 的误报（值一直正确，是文案不成立、会误导排障）；⑥`MAINTAINER.md` §8 的派生资产自检口径按**面**收敛：解释性文字可以有（本文件 + `docs/review/` 历史评审），**分发载荷（`resources/**`）与可执行面（`scripts/`、`tests/`、`SKILL.md`、安装清单）必须零命中**——上一版"只应命中解释性文字且仅在 MAINTAINER.md"仍不成立（规范正文当时也在举例），并改用 `grep -E`（BSD grep 的 BRE 不支持 `\|` 交替，实测恒空）；⑦`SKILL.md` 的派生契约示例把写死的版本号改成占位符 `derived_from_version: <你派生时本 Skill 携带的版本，如 3.21.2>`——原文是**第 5 个版本字符串且无任何断言守护**（审计只守 frontmatter / description / 横幅 / 正文版本节），下次仅按"三载体"升级时它会静默滞后且全绿，正是本仓反复警告的"声称的覆盖面 ≠ 校验的覆盖面"；⑧`agent-governance.yml` 的"读取方"注释更正：`scripts/bootstrap.sh` 是**安装期写入方且不随包下发**，原文把它列为目标仓库里的"读取方"，会让目标仓读者去找一个不存在的文件；⑨规范 §2.17 的矩阵落点句消解自相矛盾：原文"该矩阵**单落点于功能目录**（§1.2，**不双落变更目录**）"与门禁 4 的两个 glob（`docs/*/01.5` 与 `<变更根>/*/01.5`）以及**本仓 16 个 01.5 全部落在变更目录**的事实均不符——"单落点"要表达的是**不两处分叉**，现改为"有功能目录落功能目录、无功能目录（源仓即此类）落变更目录"；⑩`tests/audit-standards-src.sh` 的 **A11 体量上界按 §5.1 重校准 124 KiB → 128 KiB**——两轮对齐修正把原上界余量吃到 **193 字节**（与 v3.16.0 的 53 字节同型，上界事实上已再次失效），先压缩（删正文里的变更日志式括注、判定口径收成一句）再按 **KiB 步进 +4 KiB** 重设，余量 4,289 字节；`MAINTAINER.md` §5.1 与断言文本同步记录依据 | **已接入项目**：`--upgrade` 带过去即可，**无回填义务**（无新规范性条款；门禁判定强度、产物文件名、目录布局逐条不变）。**唯一可观测差异**：自定义 `--docs-dir` 安装不再打印那条假 NOTE。本版同时登记 **FU-030**（门禁 02 的三条锚定正则误杀行首加粗标签形态 `**回滚策略**：`——**假红**，CHG-003 / CHG-005 的历史产物即因此被判不合规；修法要改 shipped 门禁 + golden case，另起变更）、**FU-031**（门禁与文档的**双向错位**清单：文档要求而 A 层不查 3 项 / 门禁强制而文档未写 6 项 / `commit-msg` 只取首个 id / 变更号字符集 / `stop` 复用 `begin` 全部校验）、**FU-032**（`docs/` 保持忽略的**已裁定**后果：本仓 `--stage ci` 无读盘兜底必然红；`stamp-provenance.sh` 未纳入版本控制 → 克隆式安装拿不到该载荷）、**FU-033**（三处疑似绕过面：06.5 断言的第三条分支无锚定 / 溯源块 `change:` 字段未校验 / `metrics` 的 `first_commit_referencing` 全史词边界匹配） |
| v3.21.1 | 2026-09-16 | 文档与模板的**对齐修正**（2026-09-16 全项目 review：全文件遍历 + 两套自测试 + 独立评审；**不改变任何规范性要求**，无新增/删除/放宽条款）：①`AGENTS.md` 门禁第 1 步的 `00-intent.md` 节清单补「开放问题」（`begin` 硬校验该节，原文只列问题/预期结果/约束 → 照写会被拦）；②`AGENTS.md` 批次条款把「`00-governance.json` 一行一个变更」改为**推荐**（v3.20.0 起读取器格式无关，原措辞与规范 §1.1 的「推荐」不一致）；③`AGENTS.md` 锚点表与 `METHODOLOGY.md` 两处场景覆盖率门槛补 **L3 ≥90%**（规范 §2.5 阶段 4 一直如此，两文件漏写）；④`methodologies/development.md` 跨层数据结构引用改指 `data-structures.md` **§1「转换规则（M0）」**（原写 §2，该节是五项属性规范）；⑤`methodologies/data-structures.md` 两处悬空子节引用 `§4.3` 改为 **§4 第 3 条**（该文件 §4 无编号子节）；⑥`PULL_REQUEST_TEMPLATE.md` 门禁 1 补 `03.5-tasks.md` 含 TASK、门禁 4 补「03-plan 正文回写 + 编号连续」，门禁 2/3 补齐打点断言、全量回归计划、四要素与"历史批次未覆盖"（对齐 §2.16.3 DoD 唯一权威版）；⑦`06-delivery-summary.md` 定位行修正——它**不在「最低文档集八类」内**（八类＝01-spec／02+03／03.5／04.5／04／05／06.5／01.5），属 §2.5 阶段 9.5 必含产物与 FU 落点；⑧`06.5-deployment-config.md` 显式写明"未命中"声明的**行首**形态要求（`stop` 按行首匹配，写成同行加粗标签会匹配不到；模板本身不预置该行，防空模板绕过）；⑨`github-artifact-pipeline.yml` 的 03.5 骨架把任务编号由 `T-001` 改为 §1.1 认可形态 `TASK-001`，并在注释里指向 §2.5 阶段 3 验收标准（**仍不预置门禁关键词**，防空骨架绕过 `begin`）；⑩`agent-governance.yml` 读取方清单补 `stamp-provenance.sh`（它读 `provenance.include_email`），并删去对**派生 Skill 名**的引用（源仓不应出现派生资产的路径引用）；⑪`check-standards-compliance.sh` 的文档变更正则同时接受 **`change_root`** 前缀——此前只认 `<docs>/<feature>/`，变更轨不在 docs 下时会把合规产物判成缺失（**假红拦截**），与 `agent-gate`/审计的口径分叉；⑫`scripts/bootstrap.sh` 的 `change_root`/`bugs_root` 派生改写加**守卫**：仅当现值仍是内置默认 `docs/changes`／`docs/bugs` 时才随 `--docs-dir` 派生，用户显式 pin（如 `specs/changes`）不再被静默覆盖（原注释声称有此保护，实现没有），并打印 NOTE；⑬双 README 仓库树补 `.github/workflows/ci.yml`（本仓自身的 CI）与 `tests/.audit-baseline`（源层工具，之前漏列）；⑭`MAINTAINER.md` 文件角色表补上述两行、联动表补「新增/删除测试套件」行，§8「全仓 grep 派生 Skill 零命中」的说法按可判定口径重写（原文被自己举例证伪）；⑮源层审计两处注释的「八类含 06-delivery-summary」笔误一并修正（**仅注释**，断言数与语义不变） | **已接入项目**：`--upgrade` 会把这些模板/脚本带到目标仓库，**无回填义务**（不含新规范性条款，历史产物保持原样即合规）。**唯一建议动作**：若你的仓库把 `change_root` 显式 pin 在 docs 之外（如 `specs/changes`），本版起 `--docs-dir` 不再覆盖它（此前会被静默改写）；直接把 `check-standards-compliance.sh` 与 `scripts/bootstrap.sh` 一起同步即可。源层审计 263 条、golden 268 条断言**逐条不变**（本版未改任何断言） |
| v3.21.0 | 2026-09-15 | 通用层审计的**覆盖面**修复（dogfooding 发现：**审计对"产物落在变更目录"的仓库完全空转，一个带跳号的仓库通过了审计**）：①**根因——审计单元靠目录形状取全集**。`audit-docs-consistency.sh` 用"`<docs>/` 下一层的子目录"当审计单元，而 `docs/changes`、`docs/bugs`、`docs/review` 都算"子目录"；它们只要存在一个，"没有产物目录"的空转分支就不触发，G2/G3/G5/G6 于是**一条断言都不执行、也不打印任何行**——操作者读到 "4 passed" 会以为这几组都过了。**实测**：一个 `docs/changes/CHG-001/01-spec.md` 里带 REQ 跳号（`REQ-001` 与 `REQ-003` 同现、缺 `002`）的仓库**通过**了审计；②**同一根因的第二面**——G4 的两个方向都只找 `<docs>/` 下一层的 `09-changelog.md`，而变更轨的 changelog 落在 `<change_root>/<变更号>/`（深度 2），于是同时造出**假红**（"每条已登记 BUG 都被某 CHG 引用"找不到 changelog）与**空转假绿**（"每个 CHG 的 BUG 行已登记 log"一个 changelog 都没读到，计数恒为 0）；③**伴生缺陷在 `check_seq`（编号连续性）**，两处叠加，本仓 16 个 `01-spec.md` 共误报 **695** 处"缺号"：(a) 它 `grep` 全文件的 `PREFIX-N` **出现**，把正文**引用**也算成编号——CHG-006 定义 `REQ-033`~`041`（连续 9 个）却因正文写"`REQ-024`~`032`"被撑成 24..41，CHG-002 定义 `REQ-008`~`013` 却因首行写"CHG-001 已用 `REQ-001`~`007`"被撑成 1..13；(b) 基线写死 `seq 1 max`，**假定被审文件独占从 1 开始的整个编号空间**，而 §1.2 的编号空间是**全仓全局**的——任何不始于 1 的文件都被判"1..max 全是缺号"（CHG-016 定义 `REQ-080`~`083` 连续，却报 79 个缺号）；④**修法一·正信号识别**：审计单元 = 承载 `01-spec.md` / `01.5-rtvm-matrix.md` / `09-changelog.md` 之一的目录（与门禁 `check_delivery_doc` 的"看起来像功能目录"判据同源，CHG-004 为修同族假绿而引入）。`docs/bugs/<BUG-xxx>/` 以 `01-diagnosis.md` 起名，不命中信号，**刻意排除**（其一致性由门禁六件校验与 B 层 Review 承担）；⑤**修法二·空转必须显式声明**：任一组的目录集合为空时打印一行 `VACUOUS SKIP`，且 **G2 与 G3/G5/G6 分开声明**（两组覆盖面不同，合并成一句会让操作者误判"哪几组没跑"），措辞写明 `did NOT run` / `do NOT read this as a pass`——**静默空转 = 假绿，是本文件最该防的失效模式**；⑥**修法三·按"不变量是否时间不变"分轨**（本版最核心的划分）：**G2 编号连续性**是**时间不变**的（`REQ-001` 与 `REQ-003` 同现而缺 `002`，写下那天就错、今天仍错，与"当时规范怎么写"无关）→ 覆盖**功能目录 ∪ 变更目录**；**G3/G5/G6** 是拿**当前规范版本**比对（§3 归档标签 / §4 必填节 / 01.5 矩阵回填），而 §2.15 硬性规则 4 与 §2.16.5「**已闭合 CHG 禁止改写**，勘误/补充一律新开一组变更文档」**明令历史变更目录不可回填**——拿今天的 §3 去判 CHG-002 的归档清单是**时代错置**，只会产出**永久无法清除的红**（本仓实测 CHG-002 / CHG-005 / CHG-007 共 4 条，且按规范不许改；永久红比漏报更坏，它训练操作者忽略审计）→ G3/G5/G6 **只覆盖活文档（功能目录）**，变更轨的这三类一致性由**门禁在创建时**（`begin`/`staged`/`ci`/`stop`，那时产物还可修）承担。"变更目录"按**结构**判定（直接位于 `<change_root>/` 下一层），不按目录名猜——变更号允许自定义形状（含 `BUG-<UTC 时间戳>` 等事故重入号）；⑦**修法四·编号只数定义式、基线取文件自身 min**：定义式 = 编号出现在**行首**（可带 `#`~`######` 标题 / 表格行首列 / `-` 列表项标记；句中引用不计），区间基线由 `seq 1 max` 改为 `seq min max`——只要求"文件自己定义的号段无空洞"，这才是"跳号"的正确语义（`REQ-001` 与 `REQ-003` 同现仍被抓）。**实测：本仓 61 个产物文件（16 REQ + 15 DES + 16 TC + 14 SC）误报 0 处**；两处修正**缺一不可**（只改基线仍有引用污染，只改定义式仍被 1..max 判死）；⑧**修法五·G4 遍历全树 changelog**（两个方向都用 `find <docs> -name 09-changelog.md`），假红与假绿一并消除；⑨**为什么以前测不出来**——T10 的合规夹具把产物放在 `docs/feata/`（功能目录，恰好是旧实现的覆盖范围），于是"覆盖面靠夹具的目录形状成立"这件事从未被检验；这是本仓**第五次**同一族失效（v3.19.0 的 `## Observation`、v3.20.0 的单行记录、以及本版三处），共同形态是**夹具比真实产物规整**；⑩源层审计新增 **A22 ×12**（含 1 条**反向断言**：`seq 1 max` 不得回潮），golden 新增 **T20 ×9**（管线态跳号必须判红 / 空转必须显式声明 / 号段不始于 1 不算缺号 / 正文引用不得撑大号段 / G4 不假红 / 无产物目录时 G2 声明空转 / 功能目录零回归） | **同步 `tests/audit-docs-consistency.sh` 即可**（本版只改通用层审计；`scripts/agent-gate` 与 `scripts/stamp-provenance.sh` 本版未改）。**零影响**：产物落在功能目录 `docs/<feature>/` 的仓库，G2/G3/G5/G6 覆盖与判定逐条不变。**收益**：产物落在变更目录 `docs/changes/<变更号>/` 的仓库（即**启用了变更管线**的仓库）此前被审计**完全空转**——G2/G3/G5/G6 一条断言都不跑却显示通过；升级后 G2 真正覆盖两轨，且空转的组会被点名声明。**注意**：本版**不**把 G3/G5/G6 扩到冻结的变更目录（见 ⑥），因此这类仓库升级后仍会看到一行 `G3/G5/G6 VACUOUS SKIP`——那是**如实告知未覆盖**，不是失败；变更轨的 §3/§4 一致性请在门禁侧确认（`agent-gate --stage stop`） |
| v3.20.0 | 2026-09-15 | 治理记录的**格式无关性**修复（dogfooding 发现：**照着随包模板走一遍，记录会被自己的门禁拒绝**）：①**根因与回归来源**——读取治理记录的实现于 v3.18.0 为支持批次从**整文件**（v3.14.0 的 `json_string <file> <key>`：`sed -nE … "$file"` 逐行扫全文件，故多行可用）改为**行式**（`gov_record` = `grep` 取首行 + `json_field` 单行 sed）。行式对批次是**必需的**（必须按 `change_id` 隔离兄弟记录，否则读到兄弟的风险），但它**静默打破了多行格式化记录**：多行文件里 `grep` 命中的那一行只有 `"change_id": "CHG-016",`，`json_field` 在**该行内**找不到 `risk_level` → 空值；②**实测后果**（可复现 A/B：同一条记录，只差是否格式化）——**多行**：`begin` 拒绝，报文 `must declare risk_level L0, L1, L2, or L3`（而该字段就在 `change_id` 下面两行），`metrics` 输出 `"risk_level":null`；**单行**：通过。**而多行正是随包模板 `resources/templates/governance-state.json` 的形状**，也是 SKILL.md 指示 Agent 生成 `00-governance.json` 时照抄的形状——**照文档走一遍得到的记录会被自己的门禁拒绝**；③**波及面实测**：本仓自己的账本 **16/16 条**（CHG-001..016）全是多行 → 全部会被自己的门禁拒绝，`metrics` 对 16 条一律报 `null`；④**修法**——`gov_record` 先 `tr -d '\n\r'` **压平**，再按**顶层对象边界**切分（`s/\}[[:space:]]*,?[[:space:]]*\{/}\n{/g`，同时覆盖批次的无逗号相邻形态与 `jq`／JSON 数组的有逗号形态），再按 `change_id` 选中。**任意合法 JSON 写法等价**；⑤**"一行一变更"的批次写法保留，但降级为风格规则**——它不再是解析器的硬要求（仍推荐：逐变更 diff 可读），记录仍须**扁平**（不嵌套对象）：`json_field` 是单行 sed，L3 的 `release_authorized_*` 三个扁平字符串字段正为此；⑥**为什么以前测不出来**——`seed_artifacts` / `seed_batch_artifacts` 都手写**单行**记录，夹具比真实产物规整，这一类形状从未进过用例（与 v3.19.0 的 `## Observation` 盲区同型）；⑦源层审计新增 **A21 ×10**（含 1 条**反向断言**：模板**不得**被压成单行来迁就解析器——数据迁就解析器是错的方向），golden 新增 **T19 ×8**（多行独立目录 / 多行 L2 所有者校验 / 多行批次逐成员风险不串 / 单行零回归） | **同步 `scripts/agent-gate` 即可**（本版只改门禁的记录读取；`stamp-provenance.sh` 与 `audit-docs-consistency.sh` 本版未改）。**零影响**：一直写单行记录的仓库行为逐字节不变。**收益**：此前被误拒的**多行**记录（含全部照模板生成的记录）升级后立即通过；`metrics` 不再对多行记录报 `null` |
| v3.19.0 | 2026-09-15 | 批次判定的**权威名单方向**修正（端到端验证发现：批次功能被它**自己强制的内容**卡住）：①**变更集合改为正向读权威名单**——门禁新增 `gov_ids()`（读 `00-governance.json` 的 `change_id`），`change_ids_in_dir` 与 `validate_diff` 的批次分支都改用它，**不再从 `## <标题>` 反向推断**。根因：`## <标题>` 在文本层面**分不清"变更小节"与"结构小节"**，而本规范 §2.16.2 **强制** `09-changelog.md` 含 Observation 记录，作者写成 `## Observation` 时它是一条合法标题；②**修掉两个实测缺陷**（同一根因）：`--stage staged` / branch CI 拿 `"Observation"` 去查治理记录 → **拒掉一个完全合法的批次**（报文 `declares no governance record for 'Observation'`）；`metrics` 凭空多出一行 `{"change_id":"Observation"}` **幽灵变更**；③**审计 G7 判定方向随之调整**——原「锚点 ⊆ 记录」要求"批次内任一 `*.md` 的 `## <id>` 都必须有治理记录"，在批次里站不住（同上），**替换**为「每条记录在批次 `00-intent.md` 有自己的 `## <变更号>` 分节」（§2.17 管线入口按变更分节）；跨批次唯一性改用名单而非锚点。代价如实记录："写了一个没登记的 `## CHG-903` 小节"不再由审计发现——它本来也由门禁兜住（任何变更号被 begin/staged/ci 用到时 `validate_governance_state` 都会因"没有治理记录"fail-closed，比按标题形状猜更准）；④规范 §1.1 批次条款补两条：**变更集合一律从权威名单读**、**批次小节标题一律写 `## <变更号>`**（`§N：CHG-XXX` 是 `05-test-results.md` 的批次小节格式，不是变更锚点）；⑤术语表「文件溯源块」行的描述收窄到**实际强制范围**（`04.5-coding-record.md` 一件，其余产物可选）——原文写"变更产物的证据块"，与 §1.1 的实际要求不一致；⑥双 README 仓库树补标三个**不下发**模板（`intent.md` / `coding-record.md` / `governance-state.json`），与树内既有 `(NOT shipped)` 约定对齐（`bootstrap.sh` usage 早已声明它们随变更号动态生成、不在复制清单）；⑦源层审计 A20 +5 形态锚点（含 1 条反向断言钉住"反向推断"不得回潮）；golden T18 补 ④b ×3 断言，**夹具改用真实形状 `## Observation`**——旧夹具用 `#### 执行记录（ReAct）` 绕开了这个形状，故一直绿（"夹具比真实产物规整"造成的盲区） | **同步 `scripts/agent-gate` 与 `tests/audit-docs-consistency.sh` 两者即可**（`stamp-provenance.sh` 本版未改）。**不用批次的仓库零影响**：独立 `<变更号>/` 目录永远优先，且 `gov_ids()` 只在 `BATCH-*/` 分支被调用。**采用批次的仓库**：升级后原先被误拒的批次交付立即恢复通过；`metrics` 输出不再含幽灵行 |
| v3.18.0 | 2026-09-15 | 变更批次：同日合并（用户设想 D"chg-016,Bug-010 这类生成一批次文件夹的，是不是可以按天合并，当天只在对应文件追加，但要遵循规范而不是新起一套，减少检索数量"）：①**新增 `BATCH-YYYYMMDD/` 布局**——**同一天**的多个 **L0/L1** 变更可共用 `<变更根>/BATCH-YYYYMMDD/` 一个目录。**放宽的只有目录，产物文件名一字不改**（仍是 12 件同名文件），这正是"遵循规范、不是新起一套"的落点：所有按文件名检索的路径（门禁、审计、目标仓既有习惯）全部继续有效；同批变更用 `## <变更号>` 小节锚点区分；②**`00-governance.json` 改为行式 JSON**——一行一个 JSON 对象（变更号/风险/责任人各自成行）。动机：治理记录此前靠"文件内第一个 `risk_level`"取值，批次里那会读到**兄弟变更的风险与责任人**；改行式后无需引入 JSON 解析器（保持零依赖），门禁与盖章脚本各取**本变更那一行**；③**风险不放宽**——L2/L3 必须独立目录：批次共享产物会削弱"逐变更证据边界"与角色独立性，门禁按记录逐条校验并**拒绝批次内的 L2/L3**（§0.5 的独立性要求不能靠目录布局绕过）；④**解析顺序**（`change_dir` / `resolve_dir`）：独立 `<变更根>/<变更号>/`（历史布局，**永远优先**，存量仓库零影响）→ `AGENT_GUARD_CHANGE_DIR` → 含该锚点的 `BATCH-*/`；⑤**闭环语义细一档**（FU-015 同义）——`09-changelog.md` 被同批共享，"文件存在"不再等于"本变更已关闭"（否则兄弟的 changelog 会把后加入的变更一起判为已关闭，假阳性），改判**本变更自己的 `## <变更号>` 小节**是否存在；`validate_diff` 对 `BATCH-*` 文件按锚点逐个归因，`metrics` **每变更一行**（不再把整批塌缩成一个 `BATCH-*` 行）；⑥**溯源块批次语义**——批次里 `change:` 记批次名并新增 `batch_changes:` 列出成员，风险取**批次最高值**：共享文件的块不该因"这次是哪个成员盖的"而改写（否则兄弟重盖会静默改掉风险行）；⑦规范 §1.1 新增「变更批次」条款 + 术语表加行、§2.16.5 落点总表管线入口行补批次落点、`AGENTS.md` 门禁第 1 步补批次规则与锚点表补 G7；⑧**通用层审计新增 G7 批次自洽**（`audit-docs-consistency.sh`）：逐批次核对 锚点 ⊆ 治理记录、治理记录 ⊆ 锚点、风险 ∈ {L0,L1}、锚点跨批次唯一——锚点形状与门禁 `anchors_in_file` 用同一条 sed 表达式，防止"审计认得的锚点"与"门禁认得的锚点"分叉；⑨源层审计 A20 ×N 形态锚点 + golden 新增 T18；⑩**修复 bash 3.2 缺陷**：`local a="$1" b="$x/$a"` 形式在 bash 3.2 下 `$a` 尚未赋值（`local` 先展开全部词再赋值），`set -u` 下报 `unbound variable`——该形态在 `begin` 路径因 `id` 恰为全局变量而侥幸通过，**在 `--stage staged` / CI 路径上是潜伏的**（`id` 为局部变量，不进入 `$( )` 子壳），本版一并修正 | **零回填**：不建 `BATCH-*/` 的仓库行为与升级前逐字节等价（独立目录永远优先）。已接入项目须同步 `scripts/agent-gate`、`scripts/stamp-provenance.sh`、`tests/audit-docs-consistency.sh` 三者；采用批次布局时，`00-governance.json` 必须改为一行一变更（旧单行格式在独立目录下仍完全合法，**仅批次目录要求行式**） |
| v3.17.0 | 2026-09-15 | 文件溯源（用户设想 C"每个文件记录作者 / 日期时间 / 本机信息 / git 提交者，方便回溯"）：①**新增 `scripts/stamp-provenance.sh`**（进 payload 与 `--guard` 层）——把**作者 / 邮箱 / 提交者 / 提交短哈希 / 主机名 / 平台 / UTC 时间 / 风险等级 / 规范版本**从**运行环境**读出（`git config`、`git log -1`、`hostname`、`uname`、`date -u`），写入产物首个 H1 之后的 `<!-- provenance -->` 块。动机：`00-governance.json` 的 `implementation_owner` 是**手写字符串**，写什么就是什么，回溯时无法区分"真跑过"与"顺手编的"；②**交付门禁新增溯源校验**（`--stage stop` 与 branch 模式 CI 共用 `validate_delivery`）——块存在、`author`/`email`/`generated_at`/`generated_by` 四字段非空、`generated_at` 匹配 ISO-8601 日期形状、**`generated_by` 必须指向 `stamp-provenance.sh`**。第④条是收窄缺口的关键：手写块能编出 `author`，却过不了"生产者必须是脚本"这一关；**`author` 值本身是否属实不可机器验证**，本版如实承认而非假装已解决；③**不追溯既往**——只校验当前活跃变更（门禁按 active change 走），**历史产物不得回填溯源块**：回填会把 `generated_at` 伪造成"今天"，正是该块要防的伪造形态；审计 A19 对此设**反向断言**（历史目录出现任何溯源块即红）；④`.agent-governance.yml` 新增 `provenance:` 段（隐私开关 `include_email`，解析优先级 `AGENT_GUARD_PROVENANCE_EMAIL` env > 配置 > 默认 `true`）；`coding-record.md` 模板加占位块与验收勾选项；⑤写入**幂等**（整块替换，重复运行不叠加——叠加会让门禁按首块校验而误判）；工具缺失（非 git 仓、无 `hostname`）时字段降级为显式 `unknown`，**不猜、不编**；⑥规范 §1.1 新增「文件溯源」「不追溯既往」「隐私开关」三条 + 术语表加行；⑦源层审计 A19 ×22 形态锚点（含 1 条反向断言）+ golden 新增 T16 ×6；⑧脚本随 `--docs-dir` 等路径参数化生效（经 `transform_src` 重写内部路径引用）；⑨**`agent-gate` 的治理工具白名单补入 `stamp-provenance.sh`**——新增治理脚本漏加即"新仓库装完治理包**第一次 commit 被自己的门禁拦下**"（鸡生蛋死锁；本版实测踩过，症状伪装成"分歧守卫文案不匹配"，见 `docs/review/2026-09-15-skill-review.md` §7.2.2-5）；⑩`A11` 体量上界 **120 KiB → 124 KiB** 重校准（v3.16.0 已把余量吃到 53 字节；先压缩再按 KiB 步进重设，记录见 `MAINTAINER.md` §5.1） | **无存量回填义务**——新要求只作用于**活跃变更**，历史产物保持原样即合规（回填反而违规）。已接入项目升级后**唯一的行为变化**：下一次变更收尾前须跑一次 `scripts/stamp-provenance.sh <变更号>`（默认盖 `04.5-coding-record.md`），**不跑即被 `stop` / CI 拦下**。仅需 `--guard` 层重装即可获得脚本；未装 guard 层的项目不受影响 |
| v3.16.0 | 2026-09-15 | Skill 自维护三改造（用户裁定"允许 Agent 自进化，但升级不得覆盖派生的"）：①**自更新健壮化**——`--upgrade` 三处版本比对（Skill 携带 / Skill 源 vs 远端 / 目标仓已装），新增只读 `--check`（漂移即 exit 1，可入 CI）；Skill 目录非 git 克隆时不再**静默**跳过自更新（D1：原实现守卫为假时无 else 分支，用户以为已升级）；②**`--upgrade` 不再覆盖用户显式层选择**——`--core --upgrade` 保持 `core`，仅未指定层时才全量 5 层（D4：原实现无条件重写 `layers`）；③**`docs/STANDARDS_CHANGELOG.md` 归属声明**——归 Skill 所有、目标仓勿追加；`--upgrade` 检测到本地追加行即**拒绝**（fail-closed，`--force` 可越过），既不静默覆盖也不做追加式合并（D5）；④**Agent 自进化契约**——派生 Skill 在 frontmatter 声明 `derived_from: dev-standards-bootstrap` 后，安装器与升级的**每一条**写入路径跳过该目录并打印 `derived (skip)`，**`--force` 不越过**该保护；新增只读 `--derived-report`；⑤`SKILL.md` 瘦身 **15,277 → 10,738 字节（−30%）**、最长行 **1,392 → 459 字符**——删除与 `bootstrap.sh --help` 双维护的文件清单、移出时效性版本迁移清单（→ `MAINTAINER.md` §7）与维护者须知（→ `MAINTAINER.md` §2–5），新增「Agent 自进化契约」节；⑥新增 `MAINTAINER.md`（仓库根，**不随 Skill 分发**）；⑦§1.1 新增两条条款（规范升级日志归属 / Agent 自进化与派生资产）；⑧源层审计 A18 组 + golden T15 组 | **无存量回填义务**——新条款只约束 Skill 自身的分发与升级行为，不改变目标仓库的文档要求。已接入项目无需动作；仅当曾在本仓 `docs/STANDARDS_CHANGELOG.md` 追加过本地行时，`--upgrade` 会提示并需显式 `--force` 或先移出本地行 |
| v3.15.0 | 2026-09-15 | 路径根可配置（用户指令"当前配置的写为默认值，不提供就跳过使用默认"）：①`.agent-governance.yml` 新增 `paths:` 段（`docs`/`scripts`/`tests`/`githooks`）与 `bugs_root` 键，**每键默认值 = 历史写死值**，未配置即逐字节等价于升级前（零回归）；②`scripts/bootstrap.sh` 新增 `--docs-dir` / `--scripts-dir` / `--tests-dir` / `--githooks-dir`（含 `=` 形式），解析优先级 CLI > `AGENT_GUARD_<KEY>_DIR` > 目标仓配置 > 默认；③安装器把非默认根**同步写入**目标仓 `.agent-governance.yml`（并把 `change_root`/`bugs_root` 改为派生值，必要时补落该文件），并**改写模板内部的路径引用**（hooks/adapter 按路径调门禁）——落点与配置不一致即静默错位，此两步不可省；④三个门禁模板（`agent-gate` / `check-standards-compliance.sh` / `audit-docs-consistency.sh`）统一改读配置，消除"环境变量 vs 配置文件"双轨；⑤**升级保护**：`--upgrade` 的 live 文件跳过清单由**路径字面量改为逻辑键**匹配（原实现按 `docs/bugfix-log.md` 全等比较，一旦换根所有跳过即失效）；⑥**刻意不可配置**——`.github/`（平台强制位置，换名即流水线静默失效，做成旋钮是假的）与全部契约名（`AGENTS.md` 名、`agent-gate` 落点名、变更 12 件产物名、缺陷六件套名、required-check 名，可配即失去跨仓比对与迁移能力）；⑦规范 §1.1 新增「路径根可配置」「不可配置项」两条；⑧源层审计 A17 ×12 形态锚点 + A8 取值随变量化改写；⑨golden 新增 T14（配置生效/派生/零回归/路径分类/安装器落点与配置回写/live 跳过） | **零回填**：不配置 `paths:` 的老项目行为与升级前完全一致。仅当项目想换根时重跑 `bash scripts/bootstrap.sh --all --docs-dir <新根> <目标仓库>`。已接入项目升级须**同时**更新 `scripts/agent-gate`、`scripts/check-standards-compliance.sh`、`tests/audit-docs-consistency.sh` 三者（只升其一即错位） |
| v3.14.0 | 2026-09-14 | 失败模式与供应链硬化（CHG-015，用户四问：失败/安全/拦截/部分成功）：①gate **反篡改守卫**——`.agent-governance.yml` 处于待定变更中（porcelain 非空含未跟踪）时不执行其验证命令（封堵"恶意 PR 注入命令→维护者钩子自动执行"向量；已提交版本按标准仓信任模型执行）；②bootstrap CONFLICT/MISSING 退出前打印**部分安装计数**；③--upgrade 遇目标仓未提交改动**拒绝**（--force 逃生），非 git 目标 WARNING；④adapter 成功判定验证产物文件存在（exit 0 假阳性封堵）；⑤golden 重构为提交基线形态 + 反篡改例；⑥audit A16 ×2。已提交恶意内容属标准仓信任模型（等同 package.json），由 Review/CI 把关 | 已提交 yml/env 用户零影响；首次配置 yml 者提交一次或用 env |
| v3.13.0 | 2026-09-14 | 全面升级自更新（CHG-014，用户指令"文件内容变更了就要更新"）：①`--upgrade` 起步自更新 Skill 源——git 克隆且干净树时 `pull --ff-only`（GIT_TERMINAL_PROMPT=0 防挂起），实际更新则 **exec 重入新安装器**（规避运行中脚本被改写）；脏树/非 git/离线四类降级 NOTE 继续本地版；②README 树补 `update-assertion-count.sh`；③SKILL 同步范围补生成器义务；④audit A15。升级语义终态：源最新 → 目标同步，一次命令全局生效 | 源仓非 git/离线场景降级不阻断 |
| v3.12.0 | 2026-09-14 | 零命令初始化与升级模式（CHG-013，用户指令）：①SKILL.md 初始化默认 `--all` 全量自动注入（原 `--core`+逐项询问），分层 flag 语义保留供最小化/排障；②新增 `bootstrap --upgrade`：治理自有文件更新到携带版本 + 版本迁移打印 + 自动接线重跑，**live/用户文件硬清单跳过**（bugfix-log/06-delivery-summary/06.5/.agent-governance.yml——累积数据零丢失）；③SKILL 新增版本更新步骤（页脚 diff → 干净树确认 → --upgrade）与红线；④双 README 升级叙事；⑤audit A14。升级前须提交目标仓（git history 即备份） | 已接入项目：重说初始化话术即可完成版本更新 |
| v3.11.0 | 2026-09-14 | 初始化自动注入（CHG-012，用户指令"手动改为自动"）：①`bootstrap --guard` 自动接线——`core.hooksPath` 三态配置（未设→设/已设→跳/自定义→不覆盖并提示）、客户端适配器自动生成（env 检测）、验证命令自动探测（package.json/Makefile/pom/go/pyproject/Cargo 六类，仅替换占位符）预填 `.agent-governance.yml`；②gate 验证命令来源链 env → yml（占位符跳过）→ 未配置跳过，报文含来源；③双 README 门禁/管线两节重写为自动注入叙事（移除过时手工复制段），Quick Start 推荐 `--all`；④§2.17 安装器自动接线句；⑤golden +5、audit A13 ×2。剩余平台侧手工仅 GitHub Required Check 标记（bootstrap 打印指引） | 已接入项目重跑 `--guard/--all` 增量获得（.agent-governance.yml 冲突走 fail-closed --force）；AGENT_GUARD_VERIFY_COMMAND env 用户零影响 |
| v3.10.0 | 2026-09-14 | Bug 诊断与门禁设计增强（CHG-011，方法论依据 arXiv:2602.02475 AgentRx）：①§2.5 阶段 6 新增**根因分类**（六类互斥单选 + 消歧问题含否定判别 + 证据引用强制 + Inconclusive-sparingly + 可扩展）与**最早未恢复失败点**（三步扫描 + 下游症状警觉）；②阶段 4 新增**变更动态约束**（从 spec+前序产物合成不变量，六类型参考，显式失败路径强制；step-by-step 优于 one-shot 实证）；③阶段 7 新增**失败用例逐条登记**（用例/失败步/证据，验证日志思想）；④§2.13.4 新增**检查器设计规约**（结构 guard→断言裁决两段式、语义三值、显式失败路径、误报修剪、排除下游症状——固化的 BUG-001/002 教训）；⑤`bug-diagnosis.md` 模板同步（分类列 + 消歧 checklist + 最早未恢复点行）；⑥audit +2 形态锚点、A11 上界重校准 120KB（新条款受控增长）。新条款按 §2.14 存量不回溯，新 BUG 处置即生效 | 目标仓库随 SKILL 升级流程同步 `bug-diagnosis.md` 模板与规范正文 |
| v3.9.0 | 2026-09-14 | 规范瘦身达标 + 分层阅读路由（CHG-010，FU-017/018）：①正文措辞浓缩（§2.16.1/2/6、§2.1 子代理规则、阶段 3/4/5/6/7 必含清单与验收句、§2.15 规则 4、§2.6.4/2.7/2.8/2.9/2.10/2.11/2.12/2.13.3/2.16.3/2.16.5、§5 三行、§2.17 依据段等 30+ 处，条款要点逐条保留）——**149,986 → ≤115KB（117,760）达标，累计 −21.5%**，并新增审计 A11 体量上界断言防回弹；②新增「**分层阅读路由**」节（宪法/流程/操作/历史四层 → 节号 → 加载时机；物理拆分详设见 CHG-010 08-supplement）；③AGENTS.md 锚点表补路由行。无规范性条款增删（纯措辞与导航） | 目标仓库随 SKILL 升级流程同步；无存量回填义务（无新条款） |
| v3.8.1 | 2026-09-14 | 变更取号占用核实条款（CHG-008，BUG-003 事故条款）：①§2.15 硬性规则 4 增补"**取号前必须核实占用**"强制动作——先查磁盘 `docs/changes/` 现有目录、再查远端同名目录/分支，发现已占用立即改取下一个号（并行会话/多入口工作场景）；②§1.1 变更管线入口件注记同步；③源层审计增补条款存在性断言（防条款被静默移除）；④CHG-002 历史欠账补建四件交付物（04.5/05/07/09，追溯性补建标注，FU-006 闭环） | 目标仓库随 SKILL 升级流程同步；新变更起编时即须核实占用，存量变更不回溯 |
| v3.8.0 | 2026-09-14 | 规范正文压缩（CHG-005，目标 −23% 体量、零规范性要求删减）：①**升级日志外迁**至本文件（§2.14 规则改指本文件，正文移除历史节，规范正文 −26KB）；②**§3 执行清单唯一化**——§4「变更执行检查清单」归档节改为强制引用 §3（消除 20 条目逐字重复与手工同步义务，防漂移源，BUG-001 同族预防）；③§2.5 新增「阶段验收 A 层命令速查表」，16 处阶段验收行内嵌 grep 命令收拢为唯一命令权威；④§2.16.5 落点总表去重（更新语义列改为单点引用 §2.15）；⑤§5 Anti-Patterns 同源行合并（§2.16.6 三条同源拦截合一、§2.9.4 敏感数据两行合一），触发条件与处置动作全保留；⑥双 README 瘦身至 195 行/版（≤200 目标，复述改指针，树/命令表/风险矩阵实质信息保留）；⑦新增「术语与产物速查表」；⑧源层审计新增 A7 断言组（日志落点 / §3 唯一性 / 双 README 结构一致性 / 历史残留），全部负例验证 | 目标仓库随 SKILL 升级流程获得 `docs/STANDARDS_CHANGELOG.md`（bootstrap --core 同步下发）；升级日志自本版起写本文件；存量文档不回溯回填（结构重组无新规范性条款） |
| 版本 | 日期 | 变更条款 | 影响既有文档（须回填） |
|---|---|---|---|
| v3.7.0 | 2026-09-09 | 需求/变更最低文档集八类 + 缺陷文档组六件套 + 编码记录门禁：①§1.1 功能文档编号清单新增 `04.5-coding-record.md`（编码记录）与「**最低文档集八类映射**」（需求规格=01-spec / 设计=02+03 / 任务=03.5 / 编码=04.5 / 测试脚本=04 / 测试结果=05 / 配置变更=06.5 / 矩阵=01.5，缺一判未完成）；②变更组编码落文档：§2.5 阶段 5 产物与验收、§2.1 矩阵、§2.16.2 主线、§2.16.5 落点、§3 清单、§4 归档清单同步 04.5；`validate_delivery`（gate stop 与 branch CI 同层）新增 04.5 存在且非空校验；③缺陷文档组三件套扩**六件套**：新增 `04-matrix.md`（BUG↔REQ/DES/CHG/文档/TC 追踪矩阵+验证状态）/`05-config.md`（配置/DB 影响，未命中显式声明）/`06-tasks.md`（修复任务拆分+依赖+复跑 TC 归属）——§1.1/§2.5 阶段 6/§2.15 硬性规则 4/§2.16.2/§2.16.5/§2.17/§3/§5 全部三件套引用点同步；`00-governance.json` `bug_ref` 校验扩为六件；④回填清单双侧联动：CHG 侧 §4 清单 9→10 项、log 侧 8→9 项（均插入 04.5-coding-record 项），「缺陷文档组三件套」项改六件套；⑤新增模板 `coding-record.md`/`bug-matrix.md`/`bug-config.md`/`bug-tasks.md`，旧 bug 三模板定位块与验收同步；golden cases 74→80，源层审计 v3.7.0 快照同步 | 新接入项目随 Skill 复制 4 个新模板（`coding-record.md` → `docs/changes/<变更号>/` 起编时使用、`bug-matrix.md`/`bug-config.md`/`bug-tasks.md` → `docs/bugs/_templates/`）；已接入项目同步 `scripts/agent-gate` 与 `tests/run-tests.sh`；存量已闭合变更/缺陷不回溯，新变更/新缺陷从本版起执行 |
| v3.6.0 | 2026-09-09 | 变更/缺陷文档组强隔离：①§2.15 硬性规则 4 重写为「一次变更一组文档」——新变更必须分配新变更号并新建独立变更目录 `docs/changes/<新变更号>/`（如最新已用 CHG-041，新变更取 CHG-042），禁止在既有变更目录/CHG 条目上改写或追加；已闭合 CHG 与已登记 BUG 条目**完全只读**（删除「修订说明」逃生门；05-test-results 批次修订标注不受影响），勘误/补充/衍生修复一律新开一组，经 CHG「追踪矩阵映射」与缺陷组「关联缺陷」引用旧组；②缺陷文档组三件套：§2.5 阶段 6 产物升级为 `docs/bugs/<BUG-xxx>/`（`01-diagnosis.md` 诊断含同族推演 / `02-impact.md` 影响关联矩阵 / `03-test-plan.md` 测试计划），每次缺陷处置新起一组、新增模板 `bug-diagnosis.md`/`bug-impact.md`/`bug-test-plan.md`，bugfix-log 索引行加「文档组落点」列，§4「Bug 修复回填清单」7→8 项（log 侧同步 8 项）；③门禁最小增量：`00-governance.json` 新增可选 `bug_ref` 字段（缺陷修复须声明，非空时 `agent-gate begin` 校验缺陷组三件存在且非空；根目录 `AGENT_GUARD_BUGS_ROOT` 可覆盖默认 `docs/bugs`；id 正则 `^[A-Za-z0-9._-]+$` 防路径穿越），golden cases 68→74；④§5 新增两条拦截（新变更复用既有变更目录 / 缺陷未新起文档组）并收口「覆盖丢历史」行，§1.1/§2.16.2/§2.16.5/§2.17/§3/§4 联动 | 新接入项目随 Skill 复制 3 个 bug 模板到 `docs/bugs/_templates/`（每次缺陷组从模板复制）；已接入项目同步 `scripts/agent-gate` 与 `tests/run-tests.sh`；存量已闭合变更/缺陷不回溯，新变更/新缺陷从本版起执行 |
| v3.5.0 | 2026-09-09 | 治理闭环三改造：①`validate_governance_state` 拒占位符执行主体（`PENDING`/`TODO`/`TBD`/`待定`——`implementation_owner` 恒检、`test_owner`/`review_owner` 按 L2/L3），L3 新增释放授权三字段 `release_authorized_by`/`release_authorized_at`/`release_authorization_evidence`（缺一即 `agent-gate begin` 拒绝放行）；②`validate_delivery` 提取复用，branch 模式（`--stage ci --base <ref>`）对 diff 触及的变更追加 delivery 证据核验（05+09+Observation），`pre-push` 模板接线 `--stage ci`（base 取 upstream，无则退化 HEAD）；③新增 `commit-msg` 归因闸门（暂存代码文件的提交消息必须引用有效变更号；豁免：合并提交/以 Revert 开头/纯文档产物提交）+ 新模板 `commit-msg`，metrics `first_commit_referencing` 改词边界匹配（防 CHG-7/CHG-71 前缀碰撞）；④golden cases 55→68（占位符拒绝 ×3、L3 授权 ×2、branch delivery ×2、commit-msg ×5、词边界 ×2）；⑤文档同步：双 README（徽标/断言数/目录树/命令表/授权句）、SKILL 复制清单与升级流程、DS §2.17 两处与 §2.17.4；源层审计 PART B 追加 v3.5.0 快照 6 条（72→78） | 复制 `commit-msg` 模板到 `.githooks/`；更新 `pre-push` 与 `scripts/agent-gate` |
| v3.4.0 | 2026-09-08 | 融入「状态/触发链路审计」方法论：①§2.5 阶段 2 技术影响新增**隐式链路三向遍历**（正向调用点/反向隐式边 watcher·事件总线·钩子/事件源×归宿矩阵，落 02「隐式链路三向遍历」节或显式"未命中"；登记 METHODOLOGY §3.2 M0 条件命中）；②§2.5 阶段 6 Bug 修复新增**同族推演**（根因表「同族推演」行 + 「根因·同族推演」节，三处置：已同批修复/登记 FU-xxx/确认无同族；登记 METHODOLOGY §3.6 M0）；③修复防回归统计恒真缺陷：`grep -c 'BUG-'` → `grep -cE '^### BUG-[0-9]'`（模板占位 BUG-xxx 不参与统计）；④新增方法论文档 `docs/methodologies/state-trigger-audit.md`（六条教训+六步清单+收敛信号+三个反模式）；⑤同步：AGENTS 锚点（"只是改 bug"+"做影响分析"行）与 AGENTS 页脚/SKILL 复制清单/双 README（版本徽标·目录树·特性表·页脚）/bugfix-log 模板（BUG-xxx 占位注释 + 根因摘要含同族推演字段）/方法论详情文档（development·data-structures）页脚/pipeline 02 骨架注释（隐式链路提示）；阶段 8 必含清单补缺陷修复类专项核对（三反模式即退回）；⑥全局一致性收口：§2.16.2 主线代码块补阶段 2 隐式链路/阶段 6 同族推演与双登记、阶段 2 A 层 grep 收紧为「隐式链路三向遍历」单关键词（防无关"未命中"字样绕过，与③同族缺陷）、§2.15 补 00-intent/00-governance 行、§2.17 补 00-governance.json 权威定义与「产物目录双轨约定」、§2.5 总则/§1.1 补变更目录注记、§5 补同族推演拦截行、METHODOLOGY §4 L1 必须列补业务场景清单；⑦收敛审计（按本版方法论自审）：bugfix-log 占位标题改 `BUG-xxx`（③ 断言成真——原占位 BUG-001 仍匹配 `^### BUG-[0-9]`，空 log 恒真计数）、pipeline 02 注释避开 A 层关键词原文（防空骨架绕过阶段 2 验收）、§4 归档清单补角色分派项与编号顺序核对（与 §3 严格同源）、§2.16.5 落点总表补管线入口行、§2.16.2 主线补 00-intent/00-governance 与 gate begin 步骤、METHODOLOGY §4 补条件命中注记；⑧编号体系维度补漏（v3.3.0 SC 遗留漂移）：§1.1 前缀计数 9→10、§1.2 编号顺序规则与核对表补 SC/BUG、§2.16.4 自检第 3 条补 SC；⑨审计流程自身收尾三件套：升级日志重排为严格新→旧（原升序段+降序段拼接，新条目置顶对齐 bugfix-log/changelog 惯例）、新增 `tests/audit-docs-consistency.sh`（把本轮全部审计不变量固化为 50+ 机器断言：版本链/关键词落点矩阵/§3↔§4 同源/编号体系/占位 vs A 层断言防恒真/骨架 vs A 层关键词/00 管线件落点/bugfix 双登记 7+1 对齐/锚点章节/README 树↔磁盘/日志排序）并接线 SKILL 版本同步红线与双 README 目录树；脚本适用范围分层（规范源层专用、不入 SKILL 复制清单，与 agent-gate/run-tests 通用层互斥）与断言分区（PART A 永久结构不变量 / PART B 版本快照随升级更新）防误复制与脚本腐烂；⑩审计能力下沉通用层：新增模板 `resources/templates/audit-docs-consistency.sh`（复制到目标仓库 tests/，G1-G6 跨文档一致性：版本链/编号连续/§3↔§4 归档同源/bugfix 双登记互证+陈旧占位检测/RTVM 回填一致/§4 必填节完整；§2.13.4/§3 同源声明/§1.1 编号规则三处接线；fixture 正例 21 项+负例 4 类拦截验证），并修正源层审计脚本 §3↔§4 校验按 fence 序号取块导致的恒真空转（改章节标题锚定+非空断言）；⑪分层收口：run-tests.sh 扩至 52 项断言（新增 audit 模板 fixture 正例+三类负例+SKIP，§2.17.4 落实到通用审计模板）、AGENTS 锚点表新增「交付前文档互证」行、README 源层脚本条目标注"规范源层专用（不随 Skill 分发）"；⑫实现层功能审查（通读 agent-gate/compliance 全文找运行时缺陷，区别于文本一致性审查）：compliance.sh 代码扩展名清单与 agent-gate is_code_path 对齐（原 6 种漏 .sh/.sql/.kt 等 14 种——同层守卫口径分叉导致代码变更逃过文档检查）+ 误导缩进修正、通用审计器空格路径健壮化（while-read 迭代 + 引号包裹，兼容含空格目录名）、run-tests fixture 迁移 mktemp（防 repo 内残留临时目录）、agent-gate 补两条已知局限注释（staged 模式无法归属代码到具体 change、metrics 子串匹配前缀碰撞）；⑬管线-门禁闭环缺口修复：传动机制派发骨架集原缺 `03.5-tasks.md`（gate begin 必检七件之差集未覆盖，spec 合入后 begin 必失败）——pipeline yml 补 03.5 骨架（注释避开门禁关键词原文）、PR 文案改四件、§2.17.1 正文同步"骨架集=必检差集"原则；附带核查：run-tests 旧 47 断言无恒真、install-hook-adapter/pre-commit/pre-push/incident workflow 通过、development/data-structures 全部章节引用（§3.6/§3.4/§3.2/§5/§2/§4）存在且内容声明与规范一致（裁决顺序、弱类型两类例外）；⑭E2E 集成测试（SKILL 全流程装进空白仓库走 install→intent→begin→写码→staged→stop→auditor→metrics）暴露并修复两处单元测试盖不住的接缝缺陷：①治理包安装提交被自家 pre-commit 死锁（scripts/agent-gate、tests/*.sh、.githooks/ 等治理件被判产品代码，鸡生蛋）——is_code_path 增治理工具排除清单，双轨约定补声明，run-tests T4b 锁定；②metrics 在产物未提交态被 set -e 击杀（first_commit_referencing 空匹配返回 1）——显式 return 0，T7 补未提交态 golden case；断言 52→55；双轨约定补 05/09 双落点同步说明；⑮防复发收口（本项目八轮漏检类别逐条核验通用层防护，缺口补机器断言）：源层审计新增 A1（pipeline 派发骨架集必须覆盖 gate begin 必检差集，防 ⑬ 类断点复发）、A2（compliance.sh 与 agent-gate 代码扩展名口径一致，防 ⑫ 类分叉复发）、A3（README 声称断言数==run-tests 静态调用数且唯一，防 47→52→55 式多处数字漂移复发——上线即抓到目录树 1 处残留）；审计 68→72。治理元结论：规范强制力覆盖"接入仓库变更流程"，规范源仓库演进无机器拦截是本轮多轮返工根因，源仓库以三套验证+升级日志对账承担等效强度（不装 full gate：治理件全被 is_code_path 排除会空转） | 新接入项目随 Skill 复制 `state-trigger-audit.md`；已接入项目升级时同步复制（v3.3.0+ 升级流程已含 methodologies/ 目录）；bugfix-log 模板变更只影响新登记条目 |
| v3.3.0 | 2026-09-07 | 新增**业务场景覆盖率规范**（业务/流程测试全景覆盖）：①§2.5 阶段 4 新增「业务场景全景枚举与场景覆盖率」——测试 Agent 以**业务专家视角**五维枚举（用户角色/权限、业务状态与迁移、端到端流程链路含跨模块、时序与并发、异常业务路径），用户故事未明写的隐含场景同样入清单；落 04「业务场景清单」节（SC-xxx + 场景描述 + 关联 REQ + 覆盖 TC，每条 SC 至少映射一个 TC）；**场景覆盖率 = 已覆盖 SC/枚举总数 ≥80%**（L3 ≥90%，未覆盖登记 FU-xxx），未覆盖须逐条理由；②§1.1 编号表新增 SC-xxx；③阶段 7 覆盖率升级为**三指标**（行 ≥70%/分支 ≥60%/业务场景 ≥80%），05 批次必报场景覆盖率数字；④`agent-gate begin` 新增 04 含 SC- 校验（缺场景清单拒绝放行），`tests/run-tests.sh` 同步负例（**46→47 项断言**）；⑤§4 模板加场景覆盖率行、§5 新增两条拦截（缺 SC 清单/覆盖率不足无理由、SC 无 TC 映射凑数）、§3 清单/§2.16.2 主线/AGENTS 锚点/PR 模板/METHODOLOGY/双 README 同步；⑥DoD 覆盖率行升级三指标、§2.16.5 落点/§2.15 更新语义/compliance.sh CI 校验/pipeline 骨架注释同步 SC、L0 纯文档变更单条 SC 降级路径 | 已接入项目 `scripts/agent-gate` 与 `tests/run-tests.sh` 随升级同步（begin 新增 SC 校验，新变更即生效；存量变更不强制回填，重开批次须补）；04 补「业务场景清单」节；05 批次补场景覆盖率 |
| v3.2.0 | 2026-09-07 | 测试标准 QA 评审修正：①§2.5 阶段 4 覆盖维度矩阵由九类扩为**十一类**——新增「**性能与容量**」（命中 01-spec 非功能指标时必须：响应时间/吞吐/资源水位，未命中显式标注；补齐阶段 7 性能证据的用例设计入口，消除设计-证据断链）与「**并发与竞态**」（引入共享可变状态/锁/线程池/异步任务时必须，联动 methodologies/development.md §3.6，消除矩阵"如适用"与细则强制语气的冲突）；「异常与降级兜底」扩义（含**非 LLM 依赖故障注入**：超时/限流/5xx；**重放与幂等**：重复提交/消息重投/脚本重执行）；②**风险联动**：L3 变更安全维度强制（命中 PII/认证授权/对外接口不得标"不适用"）、命中性能指标时性能维度强制；③阶段 7 覆盖率补**新增代码分支 ≥60%**（JaCoCo branch，L2+ 强制出具报告——防行覆盖通过但逻辑分支未测）；④§2.9.2 prompt/模型/采样参数变更强制**版本化评估集**（固定输入 + 结构断言 + 抽样人工评分，登记 06.5，变更全量重跑记 05）；⑤门禁 2 覆盖句补矩阵指针；§2.1 矩阵行、阶段 4 验收标准、§3 清单、AGENTS.md 锚点、METHODOLOGY.md §3.4/§4、github-artifact-pipeline.yml 骨架注释、双 README 同步"十一类" | 04-test-scripts（新变更即生效；存量随 §2.14 回填评估）；05-test-results 补分支覆盖摘要（L2+）；LLM 链路建版本化评估集（06.5 登记） |
| v3.1.0 | 2026-09-07 | 新增 **Bug 修复记录日志规范**：①新增仓库级索引 `docs/bugfix-log.md`（模板 `bugfix-log.md`，追加式只增不改、新条目置顶，§2.15）——每条 Bug 登记七要素摘要（现象/根因/修复/测试证据/影响文件/文档回填清单/关联 REQ-DES-CHG），完整记录仍在 09 CHG 条目（单一权威，log 只做索引防复制漂移）；②§2.5 阶段 6 升级：BUG 编号由可选改为 Bug 修复必编（人工 `BUG-xxx` 连续递增 / 事故 `BUG-<时间戳>` 复用 §2.17.2），新增「Bug 修复记录七要素」与双登记验收（A 层 grep log 索引与回填清单；B 层索引与 CHG 一致性核对）；③§4 changelog 模板：追踪矩阵映射加 `BUG-xxx` 行 + 新增「Bug 修复回填清单」条件必填节（须更新的 md 逐项勾选：01/03/04/05/01.5/06.5/06 + log 本身）；④§1.1 BUG 行落点改双登记、§2.15 表补 log 行、§5 新增拦截（未登记 log / 回填清单缺失或与实际不符 = 未闭环）；⑤AGENTS.md 改 bug 锚点、SKILL.md 复制清单、双 README 同步。不新增机器门禁（gate 校验预留 v3.x 评估） | 已接入项目复制 `bugfix-log.md` → `docs/bugfix-log.md`；存量已闭合 Bug 不强制回溯登记，新 Bug 修复从本版起执行 |
| v3.0.0 | 2026-09-05 | 新增**方法论选型层**（内容型交付，Batch 1/2）：①新增 `docs/METHODOLOGY.md`——**M0–M3 方法论分级（唯一权威判定源）** + 阶段×方法论×适用条件×不适用场景主表 + L1 CRUD 反向判定示例（允许/禁止速查）+ LLM 链路叠加约束 + 维护规则（入表必须同时具备"适用条件"与"不适用场景"，万金油方法论不许入表）；②新增 `docs/methodologies/development.md`（SOLID/DRY/KISS/YAGNI 逐条"为什么用/为什么不用"与降级场景 + 命名/复杂度/耦合内聚/错误处理/日志/并发/资源管理七维度 + 原则冲突裁决顺序 KISS>YAGNI>DRY>SOLID + 反向检查防模式无脑套用）与 `docs/methodologies/data-structures.md`（Entity/DTO/VO/Command/Query/Event 六类模型与转换规则 + 不可变性/可空性/校验/序列化/版本兼容五属性 + **弱类型穿层禁令**（`Map<String,Object>`/`any` 禁止跨层，例外仅边界瞬态与脱敏载荷两类）+ LLM 输入输出结构分离/schema 版本化/降级结构显式建模 + 真实结构反向检查示例）；③阶段 3 技术选型、阶段 5 编码约定、§2.9 AI/LLM 增加路由指针（正文不堆内容，方法论细节独立成篇，AGENTS.md 只做路由）；④AGENTS.md 锚点表补方法论路由行；⑤**不新增机器门禁**——agent-gate/CI 不校验本层（方法论选型由阶段 3 B 层双视角核对与阶段 8 Review 把关），Router YAML 化、Evidence Schema、Outcome Gate 等骨架型改造预留 v3.x 评估（见 METHODOLOGY.md §7 暂缓项） | 目标仓库复制 `METHODOLOGY.md`→`docs/`、`methodologies/`→`docs/methodologies/`（已接入项目随升级流程同步）；选型记录落点不变（03「技术选型与决策记录」节）；编码与数据结构违规按 07-review-report 问题分级登记（M0 违反=严重） |
| v2.22.0 | 2026-09-04 | 全局 review 修正：① DoD/自检**去数字化计数**（实际条目数与文中“十项/10 项”不符，防漂移）；② **`agent-gate begin` 必检产物由 5 件扩为 7 件**——补 `02-code-impact-analysis.md`（先分析后方案强制落地）与 `03.5-tasks.md`（豁免路径兼容：显式"CHG 直接实施"标注可替代依赖/里程碑标记），02 检查补「回滚策略」第三条；③ §2.17.1 传动机制与 `github-artifact-pipeline.yml` 派发骨架**补 02**（阶段顺序 01→02→03→04），03/04 骨架注释补选型对比与覆盖维度提示；④ §2 矩阵表、§3/§4 清单阶段 2/3/4/5 项、§2.16.4 自检（新增第 8 条）、§2.16.5 落点总表（补背景调研与 ReAct 行）、PR 模板（补 ReAct 行）同步 v2.20.0/v2.21.0 要求；⑤ 修正 v2.21.0 日志笔误与 ReAct 落点句强度声明（A 层字段 / B 层逐阶段核对） | `scripts/agent-gate`（begin 必检 7 件，升级后已接入项目需同步）；全部功能文档描述层；`github-artifact-pipeline.yml` |
| v2.21.0 | 2026-09-04 | §2.16.2 新增 **ReAct 执行铁律**（Thought→Action→Observation 三段式，Thought 须引用三维影响分析与背景调研、禁止只看待改文件；Observation 须附验证命令与实际输出、不符禁止推进）；§2.5 阶段 5 加 ReAct 微循环（每次源码修改强制）；§2.16.3 DoD 新增 **ReAct 记录**条目；§4 changelog 新增「执行记录（ReAct）」必填字段（每阶段 Thought 摘要 + Observation 命令与输出表）；§3/§4 清单、§5 Anti-Patterns（"上来就改代码"🔴 严重违规）、AGENTS.md 红线联动；`agent-gate stop` 与 CI 增加 09 含 Observation 标记校验 | 全部 CHG 的 09-changelog（新变更即生效；存量随 §2.14 回填评估）；已接入项目 `scripts/agent-gate`、CI 模板随升级流程同步 |
| v2.20.0 | 2026-09-04 | §2.16.1 新增第 4 步「项目背景与业务上下文调研」（未调研不得出方案）；§2.5 阶段 2 升级为**业务/技术/风险三维影响分析**（先分析后方案强制顺序）；阶段 3 技术选型强制 **≥2 候选对比矩阵** + 业务适配论证、任务拆分升级为 **PM 标准**（关键路径/并行组/估时/里程碑/DoT/风险缓冲）；阶段 4 新增**九类覆盖维度矩阵**（业务场景/逻辑/数据/并发/安全/兼容/回归，不适用须显式标注）；§2.16.2 主线、§3 清单、§5 Anti-Patterns（片面改动、静默裁剪两条新拦截）联动；`agent-gate.sh`/`check-standards-compliance.sh` 增加 A 层标记（02 业务影响/风险、03 备选选型、03.5 依赖里程碑、04 覆盖维度） | 全部功能 02/03/03.5/04 文档（新变更即生效；存量随 §2.14 回填评估）；已接入项目 `scripts/agent-gate`、CI 模板随升级流程同步 |
| v2.19.0 | 2026-08-31 | §2.5 新增「阶段产物验收标准总则」（唯一权威定义）并为全部阶段补齐 **A/B 两层验收标准行**（A 层机器可验：编号体系/必含节 + 校验命令留痕；B 层独立角色判定，开发 Agent 不得自评）；§2.16.2 执行主线、§3/§4 清单、§5 Anti-Patterns、AGENTS.md 联动强制"逐条抄录验收标准并勾选"；`agent-gate.sh` 与 `check-standards-compliance.sh` 增加 A 层内容标记校验（防空洞骨架冒充成品） | 各功能全部阶段文档（补「验收标准」行）；已接入项目 `scripts/agent-gate`、CI 模板随升级流程同步 |
| v2.18.0 | 2026-08-26 | §2.17 变更管线自动化扩展：①新增管线入口产物 `00-intent.md`（无意图变更 begin 门禁拒绝）；②§2.17.1 传动机制（spec 合入派发 03/04 骨架、changelog 合入开发布检查单 issue）；③§2.17.2 事故重入管线（incident -> `00-intent.md` 骨架，BUG-<时间戳>）；④§2.17.3 分环境自主权授权矩阵（A0-A4，workflow 上限 A2）与客户端 Hook 兼容分层；⑤§2.17.4 治理配置 golden-case 自测试（tests/run-tests.sh）；⑥§2.17.5 管线度量（agent-gate metrics JSON Lines）；⑦补外部研究依据（[arXiv:2608.22752](https://arxiv.org/abs/2608.22752)，Compaction Cliff：一轮压缩后安全规则仅存 53%、五轮后 10%，为「治理状态外置磁盘 + 写后磁盘级验证」设计提供实证支撑） | 新变更从本版起须含 `00-intent.md`；存量变更不强制回填，重开批次时须补 |
| v2.17.0 | 2026-08-24 | 通用化改版：移除医疗/CDSS 领域特定术语与原项目残留引用——§2.9.3「医疗安全」→「AI 安全」、§2.9.4「患者数据」→「敏感数据」、门禁5/§0.5.1 L3 高风险清单改用通用 PII 表述、RTVM/CFG 矩阵示例类名泛化、§2.8/§2.9/§2.10 中原项目特定教训（CHG-006、05 §10.1、t20/t21、rainy-inside 库）改为通用典型教训表述，使规范适用于任意领域仓库 | 无需回填（术语与示例替换，不改变任何执行要求） |
| v2.16.0 | 2026-08-24 | ①新增 §0.5 变更风险分级矩阵（L0-L3），作为全文独立性/跨平台要求唯一判定源；②§2.1 新增编排 Agent 角色、Agent 标识强制格式（§2.1.7）、分歧升级机制（§2.1.8）；③新增 §2.16.6 防遗漏/防跳过强制执行规则，专门约束 AI 执行时的漏项/跳步/摘要代替行为；④§1.1 归并 BUG/CFG/DB/FU 为编号统一主表；⑤补齐 `06-delivery-summary.md`（阶段9.5）与 `08-supplement.md`（阶段9.6）必含清单；⑥修正 §2.13 子章节编号乱序；⑦DoD 补充覆盖率与风险分级判定项；⑧§4 模板 DoD/自检块改为强制引用 §2.16.3/§2.16.4，禁止另行摘要，消除三处内容漂移风险 | 各功能 `01-spec.md`（风险等级记录）、`04-test-scripts.md`（Agent 标识格式）、`06-delivery-summary.md`（新建/补全）、`08-supplement.md`（新建/补全）、`09-changelog.md`（DoD/自检块改为引用式，风险等级判定项） |
| v2.15.0 | 2026-08-24 | 门禁 5 独立验证与合入授权；§2.1 Agent 角色/子 Agent 调度/权限与独立性；CI 与例外机制；日志脱敏；DB 回滚与幂等规则校正；§4 增角色签署表并统一必填字段表述 | 各功能 `04-test-scripts.md`、`05-test-results.md`、`07-review-report.md`、`09-changelog.md`；高风险功能补 Release Owner 授权 |
| v2.14.0 | 2026-08-24 | §2.16.5 执行产物落点总表（环节→落点→语义）；前置/DoD/自检落点补全（归档 09 CHG）；§4 模板加 DoD+自检子块 | 各功能 `09-changelog.md` |
| v2.13.0 | 2026-08-24 | §2.16 变更执行主线与完成判定（AI 防漏：执行前置/主线/DoD/自检）；§3 补执行主线引用与同源声明；§4 字段引用 DoD | 各功能全部文档（执行层） |
| v2.12.0 | 2026-08-24 | §2.15 文档更新与变更逻辑（每文档追加式/活文档/不可变分类；批次绑定 CHG；配置记录只增；changelog 只增不改）；Anti-Patterns 覆盖丢历史 | 各功能全部文档 |
| v2.11.0 | 2026-08-24 | §3 清单落点规则（勾选清单归档 changelog「变更执行检查清单」字段）；§4 模板加该字段；Anti-Patterns 清单无落点 | 各功能 `09-changelog.md` |
| v2.10.0 | 2026-08-24 | §2.14 标准升级回填；阶段 3/9 CHG 回写方案正文+拆任务；阶段 7 批次四要素；checklist 必含项验收；Anti-Patterns 存量回填/方案回写 | 各功能 `03-modification-plan.md`、`03.5-tasks.md`、`05-test-results.md` |
| v2.9.0 | 2026-08-18 | 门禁 3 批次保留；阶段 1 需求来源/评审/需求变更追加式；阶段 7 覆盖率基线/性能验证/受影响清单；阶段 6 BUG-xxx；Anti-Patterns 批次覆盖 | 各功能 `05-test-results.md`、`01-spec.md`、`02-code-impact-analysis.md` |

---

*日志随规范版本页脚同步维护，全局维护责任人：geekma (geekma@gmail.com)*
