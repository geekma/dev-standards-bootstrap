# P08 · 部署总册（Deployment Master）

> **总册编号：P08** ｜ 落点：`docs/project/08-deployment-master.md` ｜ 细节唯一权威：`docs/methodologies/project-masters.md`
> **独立完整声明**：本册自含全部必含章节正文；跨册仅以引用链接补充，禁止唯一内容寄存他册；单册可独立评审。
> **更新语义**：活文档（回填 + 变更注记）——环境/配置基线/发布流程/监控变化必须同步本册（§2.7/§2.8 联动）；被更新章节末尾追加 `> 变更注记 CHG-xxx（日期）：<摘要>`，注记行只增不删。CFG/DB 逐次明细唯一权威在 `06.5-deployment-config.md`。

<!-- TEMPLATE-MARKER: 用实文填满下列各节后删除本行（§1.1） -->

## 环境拓扑
[必填] mermaid 或表：环境（dev/test/staging/prod）/ 组件 / 依赖中间件 / 访问路径。

## 配置基线索引
[必填] 表：环境 / 配置项组 / 基线版本/值摘要 / 关联 CFG（明细见 06.5）；敏感值禁止入册（只写 key 与来源）。

## 发布与回滚 runbook
[必填] 分步发布步骤 + 检查单（对齐 §2.7.1）+ 冒烟项（§2.7.2）+ 回滚决策树（§2.7.3）。

## 监控与告警清单
[必填] 表：监控项 / 指标阈值 / 告警通道 / 值班响应（对齐 §2.8）。

## 变更注记（追加式，旧行保留）
> 示例：变更注记 CHG-001（2026-09-20）：初始化总册骨架并回填现状。
