# P04 · 数据字典（Data Dictionary）

> **总册编号：P04** ｜ 落点：`docs/project/P04-data-dictionary.md` ｜ 细节唯一权威：`docs/methodologies/project-masters.md`
> **独立完整声明**：本册自含全部必含章节正文；跨册仅以引用链接补充，禁止唯一内容寄存他册；单册可独立评审。
> **更新语义**：活文档（回填 + 变更注记）——表结构/字段/CFG/DB 变化必须同步本册（§2.5 阶段 3 总册联动 ⑧）；被更新章节末尾追加 `> 变更注记 CHG-xxx（日期）：<摘要>`，注记行只增不删。CFG/DB 逐次变更明细唯一权威在 `06.5-deployment-config.md`（只增不改），本册做索引与现状总表。

<!-- TEMPLATE-MARKER: 用实文填满下列各节后删除本行（§1.1） -->

## 实体与表结构总表
[必填] 表：实体/表名 / 用途 / 关键关系 / 所属域；DDL 明细在各功能 03/06.5。

## 字段级定义（类型/必填/含义）
[必填] 核心实体字段表：字段 / 类型 / 必填 / 取值范围 / 含义 / 关联 REQ；契约字段（DTO/VO）同步。

## CFG/DB 变更索引（对齐 §2.6）
[必填] 表：CFG/DB 编号 / 变更项 / 目的 / 关联 CHG（明细见 06.5，短引用）；无则写"暂无 CFG/DB 变更"。

## 数据生命周期与脱敏
[必填] 保留期/归档/销毁策略；PII 字段与脱敏规则（对齐 §2.9.4）；无则显式声明。

## 变更注记（追加式，旧行保留）
> 示例：变更注记 CHG-001（2026-09-20）：初始化总册骨架并回填现状。
