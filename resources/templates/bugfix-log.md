# Bug 修复记录日志（Bug Fix Log）

> **定位**：仓库级 Bug 修复**追加式索引**（规范 §2.5 阶段 6 / §2.15）——每条 Bug 一个条目，新条目置顶（时间倒序），只增不改；完整的现象/分析/根因/方案/测试记录在对应功能的 `09-changelog.md` CHG 条目（规范 §4），本文件只做索引与跨功能检索，**不复制完整记录**。
> **登记时机**：Bug 修复 CHG 合入前必须登记本文件；未登记视为未闭环（规范 §5 拦截）。
> **编号**：人工发现的缺陷用 `BUG-xxx`（连续递增）；生产事故重入用 `BUG-<UTC 时间戳>`（规范 §2.17.2 自动生成，同样在本文件登记）。
> **更新语义**：已登记条目禁止改写（无「修订说明」例外，v3.6.0）；缺陷跟踪/勘误/衍生缺陷每次在 `docs/bugs/` 下**新起一组**缺陷文档目录（新 BUG-xxx 六件套，§2.15 硬性规则 4；v3.6.0 建组、v3.7.0 扩六件），并经本条目「关联缺陷」字段引用旧组。

## 登记格式（新条目追加在下方分隔线之上，时间倒序）

<!-- 每条 Bug 复制一份下方模板填写；全部字段必填（"未命中"也是有效值）。示例标题中 BUG-xxx 为字母占位，登记时替换为真实数字编号（BUG-001 连续递增 / BUG-<UTC 时间戳>）——规范阶段 6 A 层以 grep -cE '^### BUG-[0-9]' 统计真实登记数，占位不参与匹配 -->

### BUG-xxx：<一句话现象标题>（严重度 P0/P1/P2）

| 字段 | 内容 |
|---|---|
| 日期 / 发现来源 | YYYY-MM-DD / 人工测试 · 用户反馈 · 监控告警（附 incident 链接） |
| 关联变更 | `CHG-xxx`（修复所在 CHG，必填，链接 `docs/<feature>/09-changelog.md`） |
| 关联需求 / 设计 | `REQ-xxx` / `DES-xxx`；无关联时写"无（缺陷类）"，根因为设计遗漏时必须回补 DES |
| 关联缺陷 | 无 / 旧组 `BUG-xxx`（同族/衍生缺陷引用既有缺陷文档组，本次新起一组，v3.6.0） |
| 现象摘要 | ≤3 行：谁在什么场景观察到什么异常（完整复现步骤见 CHG「现象」节） |
| 根因摘要 | ≤2 行：代码行列级结论 + 同族推演处置（已同批修复/登记 FU-xxx/确认无同族；完整根因表与同族推演见 CHG「根因」节） |
| 修复摘要 | ≤2 行：修复策略一句话（完整方案与前后对比见 CHG「方案」节） |
| 影响文件 | `file1`、`file2`（完整改动清单见 CHG「方案·改动文件清单」） |
| 测试证据 | 防回归用例 `TC-xxx` → `05-test-results.md` 批次 §N（必填） |
| 文档组落点 | `docs/bugs/<BUG-xxx>/`（本次新建的缺陷文档组目录六件套；只增不改，v3.6.0 建组、v3.7.0 扩六件） |
| 状态 | 已闭环 / 观察中（24h 观察窗口结论，§2.7.3） |

**文档回填清单**（与关联 CHG「Bug 修复回填清单」中除"log 本身"外的 9 项逐项一致——log 自身的登记确认仅落 CHG 清单第 1 项；`[x]` 已更新 / `[ ] 未命中`并写理由）：
- [ ] 01-spec（需求影响）/ [ ] 03-plan 正文 / [ ] 04-test（防回归 TC）/ [ ] 04.5-coding-record（编码记录）/ [ ] 05-results / [ ] 01.5-rtvm / [ ] 06.5（配置/DB）/ [ ] 06-delivery（FU，如有） / [ ] 缺陷文档组 docs/bugs/<BUG-xxx>/ 六件套（新建只读，v3.7.0）

---
